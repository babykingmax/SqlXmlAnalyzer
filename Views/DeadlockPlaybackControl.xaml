<UserControl x:Class="SqlXmlAnalyzer.Views.DeadlockPlaybackControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SqlXmlAnalyzer.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="850">
    <materialDesign:Card Padding="16,12" materialDesign:ElevationAssist.Elevation="Dp2" Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Stepper Timeline -->
                <RowDefinition Height="12"/>  <!-- Spacing -->
                <RowDefinition Height="Auto"/> <!-- Controls and Description -->
            </Grid.RowDefinitions>

            <!-- 1. Stepper Timeline -->
            <Grid Grid.Row="0">
                <!-- Connecting Line behind bubbles -->
                <Rectangle Height="2" Fill="{DynamicResource MaterialDesignDivider}" VerticalAlignment="Center" Margin="24,0"/>

                <!-- Steps ListBox -->
                <ListBox ItemsSource="{Binding PlaybackSteps}" 
                         SelectedValue="{Binding CurrentStep, Mode=TwoWay}" 
                         SelectedValuePath="StepIndex"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                         HorizontalAlignment="Center">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="10,0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:DeadlockStepItem}">
                            <Grid ToolTip="{Binding ToolTip}" Background="Transparent">
                                <Border Width="28" Height="28" CornerRadius="14" BorderThickness="2" 
                                        x:Name="BubbleBorder" 
                                        Background="{DynamicResource MaterialDesignPaper}" 
                                        BorderBrush="{DynamicResource MaterialDesignDivider}" 
                                        VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="11" FontWeight="Bold" Foreground="Gray" x:Name="BubbleText"/>
                                </Border>
                            </Grid>
                            <DataTemplate.Triggers>
                                <!-- Active steps (completed) -->
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter TargetName="BubbleBorder" Property="Background" Value="{DynamicResource MaterialDesignPrimaryBrush}"/>
                                    <Setter TargetName="BubbleBorder" Property="BorderBrush" Value="{DynamicResource MaterialDesignPrimaryBrush}"/>
                                    <Setter TargetName="BubbleText" Property="Foreground" Value="White"/>
                                </DataTrigger>

                                <!-- Current step (active and highlighted) -->
                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                    <Setter TargetName="BubbleBorder" Property="Background" Value="{DynamicResource MaterialDesignPrimaryBrush}"/>
                                    <Setter TargetName="BubbleBorder" Property="BorderBrush" Value="{DynamicResource MaterialDesignPrimaryBrush}"/>
                                    <Setter TargetName="BubbleBorder" Property="Width" Value="32"/>
                                    <Setter TargetName="BubbleBorder" Property="Height" Value="32"/>
                                    <Setter TargetName="BubbleBorder" Property="CornerRadius" Value="16"/>
                                    <Setter TargetName="BubbleText" Property="Foreground" Value="White"/>
                                    <Setter TargetName="BubbleText" Property="FontSize" Value="12"/>
                                    <!-- Add dynamic soft shadow or border effect for current node -->
                                    <Setter TargetName="BubbleBorder" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="#1976D2" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                                <!-- Victim step (the final deadlocked process selected to be killed) -->
                                <DataTrigger Binding="{Binding IsVictim}" Value="True">
                                    <Setter TargetName="BubbleBorder" Property="BorderBrush" Value="{DynamicResource CriticalBrush}"/>
                                    <Setter TargetName="BubbleText" Property="Foreground" Value="{DynamicResource CriticalBrush}"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsVictim}" Value="True"/>
                                        <Condition Binding="{Binding IsActive}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="BubbleBorder" Property="Background" Value="{DynamicResource CriticalBrush}"/>
                                    <Setter TargetName="BubbleBorder" Property="BorderBrush" Value="{DynamicResource CriticalBrush}"/>
                                    <Setter TargetName="BubbleText" Property="Foreground" Value="White"/>
                                    <Setter TargetName="BubbleBorder" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="#D32F2F" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </MultiDataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <!-- 2. Controls and Description -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Controls -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Navigation Actions -->
                    <Button Command="{Binding ResetCommand}" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Padding="0" ToolTip="重置">
                        <materialDesign:PackIcon Kind="SkipPrevious" Width="18" Height="18" Foreground="{DynamicResource MaterialDesignBody}"/>
                    </Button>
                    <Button Command="{Binding StepBackwardCommand}" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Padding="0" Margin="4,0" ToolTip="上一步">
                        <materialDesign:PackIcon Kind="StepBackward" Width="16" Height="16" Foreground="{DynamicResource MaterialDesignBody}"/>
                    </Button>
                    <Button Command="{Binding PlayCommand}" Width="76" Height="32" Style="{StaticResource MaterialDesignRaisedButton}" FontWeight="Bold" FontSize="11" Margin="2,0">
                        <TextBlock Text="{Binding PlayButtonText}"/>
                    </Button>
                    <Button Command="{Binding StepForwardCommand}" Style="{StaticResource MaterialDesignIconButton}" Width="32" Height="32" Padding="0" Margin="4,0" ToolTip="下一步">
                        <materialDesign:PackIcon Kind="StepForward" Width="16" Height="16" Foreground="{DynamicResource MaterialDesignBody}"/>
                    </Button>

                    <Rectangle Width="1" Height="20" Fill="{DynamicResource MaterialDesignDivider}" Margin="12,0" VerticalAlignment="Center"/>

                    <!-- Filter CheckBox -->
                    <CheckBox IsChecked="{Binding FocusCriticalPath}" Content="聚焦关键环" VerticalAlignment="Center" Foreground="{DynamicResource CriticalBrush}" FontWeight="SemiBold" Margin="0,0,12,0" ToolTip="过滤死锁图，仅保留死锁环中的直接冲突进程和资源"/>

                    <!-- Speed ComboBox -->
                    <TextBlock Text="速度:" VerticalAlignment="Center" Foreground="Gray" FontSize="11" Margin="4,0,6,0"/>
                    <ComboBox SelectedValue="{Binding PlaybackSpeed}" SelectedValuePath="Tag" Width="70" Height="24" VerticalContentAlignment="Center" FontSize="11">
                        <ComboBoxItem Content="慢 (2s)" Tag="2000"/>
                        <ComboBoxItem Content="中 (1s)" Tag="1000"/>
                        <ComboBoxItem Content="快 (0.4s)" Tag="400"/>
                    </ComboBox>
                </StackPanel>

                <!-- Right: Description -->
                <materialDesign:Card Grid.Column="2" Padding="12,8" Background="#0A000000" materialDesign:ElevationAssist.Elevation="Dp0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentStepDescription}" FontSize="12" Foreground="{DynamicResource MaterialDesignBody}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl>

<Window x:Class="SqlXmlAnalyzer.Views.RuleConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="规则配置" Width="1180" Height="840" MinWidth="980" MinHeight="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}" Foreground="{DynamicResource MaterialDesignBody}">
    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="0,0,8,0"/><Setter Property="Padding" Value="12,5"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="150"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="规则配置" FontSize="24" FontWeight="SemiBold"/>
            <TextBlock Text="{Binding ScopeDescription}" TextWrapping="Wrap" Margin="0,8,0,12"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <TextBox Text="{Binding FilePath, Mode=OneWay}" IsReadOnly="True" AutomationProperties.Name="配置文件路径" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="LoadConfiguration" Content="载入配置…" Click="Load_Click" IsEnabled="{Binding CanEdit}"/>
                <Button x:Name="SaveConfiguration" Content="保存文件" Click="Save_Click" IsEnabled="{Binding CanSave}"/>
                <Button Content="另存为新文件…" Click="SaveAs_Click" IsEnabled="{Binding CanApply}"/>
                <Button x:Name="ResetConfiguration" Content="恢复默认草稿" Click="Reset_Click" IsEnabled="{Binding CanEdit}"/>
            </StackPanel>
        </StackPanel>
        <DockPanel Grid.Row="2" Margin="0,12">
            <CheckBox DockPanel.Dock="Right" Content="仅显示变更" IsChecked="{Binding OnlyChanged}" Margin="16,0"/>
            <TextBlock Text="筛选规则" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBox x:Name="RuleFilter" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"
                     AutomationProperties.Name="筛选规则" ToolTip="按规则 ID、类别、作用域或描述筛选"/>
        </DockPanel>
        <DataGrid x:Name="RulesGrid" Grid.Row="3" ItemsSource="{Binding VisibleRows}" IsEnabled="{Binding CanEdit}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  HeadersVisibility="Column" EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  AutomationProperties.Name="规则列表" RowHeight="42">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="启用" Width="68">
                    <DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <CheckBox IsChecked="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center"
                                  AutomationProperties.Name="{Binding RuleId, StringFormat=启用 {0}}"/>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="规则 ID" Binding="{Binding RuleId}" Width="280">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="ToolTip" Value="{Binding RuleId}"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="160">
                    <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock">
                        <Setter Property="ToolTip" Value="{Binding Category}"/>
                    </Style></DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="范围" Binding="{Binding Scope}" Width="70"/>
                <DataGridTemplateColumn Header="严重度覆盖" Width="145">
                    <DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <ComboBox ItemsSource="{Binding SeverityChoices}" SelectedItem="{Binding SeveritySelection, UpdateSourceTrigger=PropertyChanged}"
                                  AutomationProperties.Name="{Binding RuleId, StringFormat=严重度 {0}}" Margin="4,0"/>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="默认基准" Binding="{Binding DefaultSeverity}" Width="85"/>
                <DataGridTemplateColumn Header="说明" Width="*" MinWidth="180">
                    <DataGridTemplateColumn.CellTemplate><DataTemplate>
                        <TextBlock Text="{Binding Description}" ToolTip="{Binding Description}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    </DataTemplate></DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="4" Margin="0,12,0,8">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <GroupBox Header="变更预览（相对于已载入文件）" Margin="0,0,8,0">
                <TextBox Text="{Binding Preview, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </GroupBox>
            <GroupBox Grid.Column="1" Header="兼容性提示（未知项只读保留）">
                <TextBox Text="{Binding Notices, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </GroupBox>
        </Grid>
        <StackPanel Grid.Row="5">
            <TextBlock x:Name="ConfigurationState" Text="{Binding ActiveConfiguration}" TextWrapping="Wrap" Margin="0,0,0,6"/>
            <TextBlock x:Name="OperationStatus" Text="{Binding Status}" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,0,0,12"/>
        </StackPanel>
        <StackPanel Grid.Row="6" Orientation="Horizontal">
            <Button x:Name="ApplyConfiguration" Content="应用到本次会话" Click="Apply_Click" IsEnabled="{Binding CanApply}"/>
            <Button x:Name="RecalculatePlan" Content="重新分析当前计划" Click="Recalculate_Click" IsEnabled="{Binding CanRecalculate}"/>
            <Button Content="取消当前操作" Click="Cancel_Click" IsEnabled="{Binding IsBusy}"/>
        </StackPanel>
    </Grid>
</Window>

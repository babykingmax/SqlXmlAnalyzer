<Window x:Class="SqlXmlAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SqlXmlAnalyzer"
        xmlns:vm="clr-namespace:SqlXmlAnalyzer.Core.ViewModels"
        xmlns:views="clr-namespace:SqlXmlAnalyzer.Views"
        xmlns:conv="clr-namespace:SqlXmlAnalyzer.Converters"
        Title="{Binding AppTitle}"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        
        Height="900" Width="1400"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        DragEnter="Window_DragEnter"
        Drop="Window_Drop"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{DynamicResource MaterialDesignPaper}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Views/MainWindowResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/> <!-- 标题栏 -->
            <RowDefinition Height="*"/>  <!-- 内容区 -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 自定义沉浸式标题栏 -->
        <views:ShellTitleBar Grid.Row="0"
                             TitleBarMouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                             MinimizeClicked="Minimize_Click"
                             MaximizeClicked="Maximize_Click"
                             CloseClicked="Close_Click"/>

        <!-- 主内容区: 左侧 Navigation Rail + 右侧 TabControl -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="64"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 现代化侧边导航 (Navigation Rail) -->
            <views:ShellNavigationRail x:Name="NavigationRail"
                                       Grid.Column="0"
                                       OpenDeadlockClicked="OpenDeadlockFile_Click"
                                       OpenPlanClicked="OpenPlanFile_Click"
                                       GenerateHtmlReportClicked="GenerateHtmlReport_Click"
                                       ExportWordClicked="ExportToWord_Click"
                                       ExportPdfClicked="ExportToPdf_Click"
                                       ExportObfuscatedPlanClicked="ExportObfuscatedPlan_Click"
                                       CopyAnalysisResultClicked="CopyAnalysisResult_Click"
                                       ClearResultsClicked="ClearResults_Click"
                                       ThemeToggled="ThemeToggle_Click"
                                       AboutClicked="About_Click"/>
            
            <!-- 主内容区 Tabs -->
<TabControl Grid.Column="1" Margin="2" Name="MainTabControl" SelectedIndex="0" Background="{DynamicResource MaterialDesignPaper}">

            <!-- ==================== 死锁分析 Tab (A) ==================== -->
            <TabItem Header="🔴 死锁分析" Margin="0,0,-16,0">
                <views:DeadlockWorkspaceView x:Name="DeadlockWorkspace"
                                             ProcessesSelectionChanged="DeadlockProcessesList_SelectionChanged"
                                             ResourcesSelectionChanged="DeadlockResourcesList_SelectionChanged"
                                             XelSelectionChanged="XelDeadlockSelector_SelectionChanged"
                                             ToggleLeftClicked="ToggleLeft_Click"
                                             ToggleRightClicked="ToggleRight_Click"
                                             ZoomToFitClicked="ZoomToFitDeadlock_Click"
                                             PlaybackModeChecked="PlaybackModeToggle_Checked"
                                             PlaybackModeUnchecked="PlaybackModeToggle_Unchecked"
                                             CopyDeadlockMermaidClicked="CopyDeadlockMermaid_Click"
                                             OpenDeadlockMermaidClicked="OpenDeadlockMermaidInBrowser_Click"
                                             PatternsSelectionChanged="DeadlockPatternsListBox_SelectionChanged"/>
            </TabItem>

            <!-- ==================== 执行计划分析 Tab (B) - 参考 Plan Explorer ==================== -->
            <TabItem Header="📈 执行计划分析" Margin="24,0,-42,0">
                <views:PlanWorkspaceView x:Name="PlanWorkspace"
                                         LeftPanelExpanded="LeftPanel_Expanded"
                                         LeftPanelCollapsed="LeftPanel_Collapsed"
                                         RightPanelExpanded="RightPanel_Expanded"
                                         RightPanelCollapsed="RightPanel_Collapsed"
                                         OperatorTreeSelectedItemChanged="PlanOperatorTree_SelectedItemChanged"
                                         VisualTreeSelectedItemChanged="PlanVisualTree_SelectedItemChanged"
                                         GraphNodeSelected="PlanNodifyGraph_NodeSelected"
                                         GraphNodeDoubleClicked="PlanNodifyGraph_NodeDoubleClicked"
                                         CopyRefactoredSqlClicked="CopyRefactoredSql_Click"
                                         CompareSqlClicked="CompareSql_Click"
                                         CopyIndexDdlClicked="CopyIndexDdl_Click"
                                         CopyDeploymentBundleClicked="CopyDeploymentBundle_Click"
                                         CopyRollbackDdlClicked="CopyRollbackDdl_Click"/>
            </TabItem>
            <!-- ==================== A/B 并排对比 Tab (C) ==================== -->
            <TabItem Header="⚖️ A/B 比对分析" Margin="42,0,-60,0">
                <views:PlanComparisonWorkspaceView x:Name="PlanComparisonWorkspace"
                                                   TuningHistoryMouseDoubleClicked="TuningHistoryListView_MouseDoubleClick"
                                                   SaveSessionClicked="SaveSession_Click"
                                                   LoadSessionClicked="LoadSession_Click"
                                                   SwapPlanABClicked="SwapPlanAB_Click"/>
            </TabItem>
        </TabControl>
        </Grid>

        <!-- 状态栏 -->
        <views:ShellStatusBar x:Name="ShellStatus" Grid.Row="2"/>
    </Grid>
</Window>

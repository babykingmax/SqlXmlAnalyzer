# SqlXmlAnalyzer 用户使用指南 📘

欢迎使用 **SqlXmlAnalyzer**。它旨在成为每一位 DBA、后端开发人员和架构师在调优 SQL Server 查询时最得力的助手。

## 1. 打开文件 📂

SqlXmlAnalyzer 支持两种格式的 SQL Server 诊断文件：
*   **`.sqlplan`**: 包含查询语句的真实/预估执行计划。
*   **`.xdl`**: 包含死锁信息的 XML 死锁图谱。

**操作方式：**
1.  在主界面点击左上角的 **"打开 SQL 执行计划或死锁文件..."** 按钮。
2.  直接从资源管理器中 **拖拽 (Drag & Drop)** 文件到软件窗口内即可极速加载！

---

## 2. 认识主界面布局 🖥️

成功加载 `.sqlplan` 文件后，您将看到精心排版的三个主要区域：

### 2.1 顶部面板：SQL 语句
*   显示当前执行计划对应的完整 T-SQL 语句。
*   为了确保更大的画布区域，此面板**默认处于自动折叠状态**。您可以在需要阅读原始 SQL 时点击下拉箭头将其展开。
*   代码自带彩色高亮，且突破了文本截断限制，能够完美显示长达数万行的史诗级 SQL。

### 2.2 中心画布：图形化操作符树 (由 Nodify 引擎驱动)
这是整个工具的核心地带！
*   **鼠标操控与无极缩放**：按住鼠标中键或左键空白处可以任意拖拽漫游；滚动鼠标滚轮可对整个庞大计划进行**无极缩放 (Zoom In/Out)**。
*   **节点与折叠树**：每个算子（如 Index Seek, Hash Match）配有 SSMS 原生高清图标。您可以点击节点下方的 **`[+]`/`[-]` 按钮** 来手动折叠或展开复杂的执行计划分支，工具拥有绝对防穿透的完美点击体验。
*   **✨ 智能折叠与全部展开**：
    *   在工具栏点击 **“智能折叠”**，工具会自动扫描整棵树，把成本微不足道（开销<5%）且没有任何性能报警的“健康分支”一键收起，让您的视野直接穿透到最严重的性能瓶颈！
    *   点击 **“全部展开”** 则可随时恢复全局视图。
*   **红色报警节点**：若某个算子的边框被标为 **大红色**，意味着规则引擎检测到了极端的性能灾难（如：预估与实际行数偏离百倍、严重内存溢出等）。
*   **连线数据流**：节点间连线的粗细直观反映了数据流的吞吐量，连线越粗，性能往往越值得关注。

### 2.3 左侧面板：诊断报告与索引沙盒 🚨
*   **执行计划深度诊断报告**：由内置 30项专家规则驱动。这就像有一位资深 DBA 站在您的身旁，以纯中文向您娓娓道来（例如：“检测到标量函数让 SQL 引擎做盲目估算，强烈建议改为 #临时表”）。
*   **索引调优沙盒 (Index Sandbox)**：如果引擎探测到了确实存在的缺失索引，会将其列出并基于读写比及表数据行数进行智能打分（Score）。它甚至为您做好了规避“假缺失索引”坑的准备。

### 2.4 右侧面板：属性网格 📊
*   **节点属性详情**：当您在中心画布**点击任意一个算子**时，这里会列出它底层隐藏的所有 XML 属性（预估行、实际行、过滤谓词、输出列表等）。
*   **分区扫描变红预警**：如果您点击的是一个分区表扫描节点，当探测到发生了 **跨越所有分区的全表扫描** 时，`Partition Range` 一行将会以 **醒目的红色 (`#FF0000`)** 呈现！

---

## 3. 死锁分析器 (Deadlock View) 💀

若您加载的是 `.xdl` 文件，界面将自动切换至死锁分析模式：
*   **资源矩阵与死锁图**：通过原生的全景 N 节点双向环形拓扑图，清晰展示进程与独立物理锁资源之间的 `Request` (请求) 与 `Owner` (持有) 关系。
*   **✨ 死锁可视化回放 (Timeline Playback)**：告别复杂的静态 XML 阅读！界面上方提供了完全沉浸式的“回放模式”。
    *   点击“回放模式”开关后，死锁图会褪去所有颜色。点击“▶ 播放”按钮或拖动时间滑块，一步步在屏幕上亲眼见证每一条有向边的先后生成顺序！
    *   下方显示每一步锁竞争的精确中文解说，理清谁先抢占了资源，谁又发起了阻塞请求。
*   **🎯 聚焦关键路径**：勾选“聚焦关键路径”，工具会自动利用有向图深度优先搜索算法（DFS），过滤掉未参与成环的背景噪音节点。
*   **Victim 追踪**：一眼看穿哪一个事务最终不幸成为了被 SQL Server 选中杀死的受害者 (红色警戒光晕高亮)。

## 4. 快捷功能与导出 📤

在中心画布顶部的工具栏中：
*   **【导出为图片】**：支持将当前调整好视角、放大比例完美的执行计划树，一键导出为高清 `.png` 截图，直接用于您的技术报告或 PPT 汇报。
*   **【复制 Mermaid 代码】/【在浏览器查看图形】**：一键将庞大计划树转化为标准 Mermaid JS 流程图文本，方便粘贴入您的 GitHub README 或是 Wiki 文档。

# SqlXmlAnalyzer 使用说明文档

## 1. 软件简介
**SqlXmlAnalyzer** 是一款轻量、强大的 SQL Server 性能诊断桌面客户端。它不仅能帮助你直观地阅读枯燥复杂的 XML 执行计划，更是内置了一位“虚拟 DBA 专家”，自动为你扫描并指明隐藏在成百上千个节点中的性能炸弹。

## 2. 快速入门指南

### 2.1 启动与加载文件
- **启动程序**: 双击运行 `SqlXmlAnalyzer.exe` 即可启动。
- **加载计划**: 
  - 通过菜单栏点击 `文件 (File) -> 打开 (Open)` 选择文件。
  - **支持的文件格式**:
    - `.sqlplan`: SQL Server 实际/预估执行计划。
    - `.xdl`: SQL Server 死锁日志图。

### 2.2 图形界面交互
本工具完全复刻了商业软件 SQL Sentry Plan Explorer 的极简专业风格。
- **移动与缩放**: 按住鼠标左键拖动空白处即可平移视角；滚动鼠标滚轮可以无级缩放执行树。
- **信息悬浮提示 (Tooltip)**: 将鼠标悬停在任何操作符节点上，即可展开该节点的深度指标面板（如：预估/实际行数、物理读取、分区范围、并行线程数据倾斜状态等）。
- **双击看全貌**: 双击任意节点，可以在侧边栏弹出原生原始的完整 XML 片段。

## 3. 核心功能特性

### 3.1 “智能红绿灯” 性能告警视觉系统
系统内置了诊断规则引擎，它在加载文件的一瞬间就已经检查完所有的算子了。
- **🔴 红色边框 (Critical)**: 代表极为严重的性能反模式。例如：因为**隐式类型转换 (Implicit Conversion)** 导致索引失去了效用，不得不转为全表扫。
- **🟠 橙色边框 (Warning)**: 代表应当注意的潜在问题。例如：识别到高开销的 `Key Lookup` (书签查找)，或者**参数嗅探 (Parameter Sniffing)** 导致的编译值与运行值不符。
- **无边框 (Info)**: 常规健康节点。
> *小贴士*: 把鼠标移动到带颜色边框的节点上，悬浮框内会直接打印出具体的修复建议。

### 3.2 缺失索引与聚合报告
在主界面的侧边栏或底部标签中，您可以查看当前整个计划的 **专家诊断报告**。
这里会为您汇总整个查询语句中最影响性能的前 5 个高开销节点、缺失索引 (Missing Indexes) 的创建建议脚本以及基数预估误差报告。

### 3.3 死锁 (Deadlock) 分析雷达
当您导入 `.xdl` 文件时：
- 系统会自动渲染出一张精美清晰的进程与资源依赖图。
- 我们独创性地深度提取了死锁底层数据中的 `logused` 属性。在图表的进程节点上，会直接展示出该进程的**回滚成本 (Rollback Cost)**。
- 借此，DBA 可以通过图形直观地看出为什么 SQL Server 选择了当前进程作为牺牲品 (Victim)。

---

## 4. CLI 命令行与批处理高级模式

对于维护成百上千台服务器或者集成到 CI/CD 流水线的架构师，本工具提供**静默的无头命令行扫描能力**。您可以打开 Powershell 或 CMD 执行。

### 4.1 可用参数列表

| 命令 | 参数格式 | 功能描述 |
| :--- | :--- | :--- |
| **单文件分析** | `--analyze "C:\test.sqlplan"` | 对单个执行计划/死锁文件进行分析，在控制台打印报告。 |
| **多文件批处理** | `--batch "C:\PlanFolder"` | **[专家推荐]** 递归扫描目标文件夹，自动对所有 `.sqlplan` 和 `.xdl` 文件执行批量诊断并汇总成功与失败统计。 |
| **导出格式** | `--export json/txt` | (配合 analyze 使用) 将报告输出格式设定为 JSON 结构化文本或纯文本。 |
| **导出文件** | `--out "C:\report.json"` | 将控制台诊断结果保存到特定文件中。 |
| **帮助** | `--help` 或 `-h` | 打印出所有可用的命令行参数说明。 |

### 4.2 批处理实战演练
当系统变慢并产生了大量的执行计划记录，你只需要一行命令：
```powershell
.\SqlXmlAnalyzer.exe --batch "D:\SQL_Performance_Dumps"
```
工具将在一瞬间吃掉几百个文件，并利用内置引擎输出每一个文件中存在的“烂代码”（如死锁回滚成本、参数嗅探警告）。

## 5. 故障排除与日志
- 当程序发生预期外的界面白屏或者无法加载时，您可以前往程序的根目录下寻找 `log` 文件夹。
- 里面会按照日期和时间滚动记录下如 `SqlXmlAnalyzer_20260612_103000.log` 的详细追踪日志，您可以直接提交给开发者进行排错诊断。

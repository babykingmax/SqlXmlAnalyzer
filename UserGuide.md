# SqlXmlAnalyzer 用户使用指南 📘

欢迎使用 **SqlXmlAnalyzer**。它旨在成为每一位 DBA、后端开发人员和架构师在调优 SQL Server 查询时最得力的助手。

## 1. 打开文件 📂

SqlXmlAnalyzer 支持两种格式的 SQL Server 诊断文件：
*   **`.sqlplan`**: 包含查询语句的真实/预估执行计划。
*   **`.xdl`**: 包含死锁信息的 XML 死锁图谱。

**操作方式：**
1.  在主界面点击左上角的 **"打开 SQL 执行计划或死锁文件..."** 按钮。
2.  直接从资源管理器中 **拖拽 (Drag & Drop)** 文件到软件窗口内即可极速加载！

---

## 2. 认识主界面布局 🖥️

成功加载 `.sqlplan` 文件后，您将看到精心排版的三个主要区域：

### 2.1 顶部面板：SQL 语句
*   显示当前执行计划对应的完整 T-SQL 语句。
*   为了确保更大的画布区域，此面板**默认处于自动折叠状态**。您可以在需要阅读原始 SQL 时点击下拉箭头将其展开。
*   代码自带彩色高亮，且突破了文本截断限制，能够完美显示长达数万行的史诗级 SQL。

### 2.2 中心画布：图形化操作符树
这是整个工具的核心地带！
*   **鼠标操控**：您可以按住鼠标左键在画布上任意拖拽漫游；滚动鼠标滚轮可以对整个庞大计划进行**无极缩放 (Zoom In/Out)**。
*   **节点解读**：每个算子（如 Index Seek, Hash Match）均配有 SSMS 原生高清图标。图标上方是开销占比百分比，右侧是具体的逻辑与物理算子名称。
*   **连线粗细**：不同节点之间连线的粗细直观反映了数据流。连线越粗，意味着有更多的数据行 (Rows) 在此流转，这通常是性能优化的第一关注点。
*   **红色报警节点**：若某个算子的边框被标为 **大红色**，意味着规则引擎检测到了极端的性能灾难（如：预估与实际行数偏离百倍、严重内存溢出等）。

### 2.3 左侧侧边栏：操作符树目录
*   以经典树状结构 (`TreeView`) 呈现每一个节点，适合对图形树进行文字级别的概览检索。此面板也默认折叠。

### 2.4 右侧面板：属性网格与诊断报告 🚨
*   **节点属性详情**：当您在中心画布**点击任意一个算子**时，这里会列出它底层隐藏的所有 XML 属性（预估行、实际行、过滤谓词、输出列表等）。
*   **分区扫描变红预警**：如果您点击的是一个分区表扫描节点，当 `Partitioned` 属性为 True 且探测到发生了 **跨越所有分区的全表扫描** 时，`Partition Range` 一行将会以 **醒目的红色 (`#FF0000`)** 呈现！请警惕您的查询是否遗漏了分区键。
*   **执行计划深度诊断报告**：由内置 30项专家规则驱动。这就像有一位资深 DBA 站在您的身旁，以纯中文向您娓娓道来：
    *   *“检测到标量函数让 SQL 引擎做盲目估算，强烈建议改为 #临时表。”*
    *   *“检测到嵌套循环的执行次数突破了 50000 次，建议考虑 Hash Join！”*
    *   *“注意：查询未走多线程，因为 MAXDOP 被强制设定为 1。”*

---

## 3. 死锁分析器 (Deadlock View) 💀

若您加载的是 `.xdl` 文件，界面将自动切换至死锁分析模式：
*   **资源矩阵**：清晰展示哪个进程（Process ID）持有了什么锁（例如 X 锁，U 锁），以及它们在等待什么锁。
*   **Victim 追踪**：一眼看穿哪一个事务最终不幸成为了被 SQL Server 选中杀死的受害者 (Victim)。
*   **SQL 重构建议**：提取导致死锁的竞争 SQL 批处理代码，方便您排查是否由于缺少索引导致的 Range Scan 或锁升级诱发了死锁。

## 4. 快捷功能与导出 📤

在中心画布顶部的工具栏中：
*   **【导出为图片】**：支持将当前调整好视角、放大比例完美的执行计划树，一键导出为高清 `.png` 截图，直接用于您的技术报告或 PPT 汇报。
*   **【复制 Mermaid 代码】/【在浏览器查看图形】**：点击后，软件会自动遍历所有算子并将其编译为标准 Mermaid JS 流程图语法放入剪贴板。您可以直接将其粘贴入您的 GitHub README 或是支持 Mermaid 的内网 Wiki 中！

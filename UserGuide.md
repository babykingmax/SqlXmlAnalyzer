# SqlXmlAnalyzer 用户使用指南 📘

## 执行计划工作区更新（2026-09-11）

当前执行计划界面以“计划图 / 热点算子”为主视图，问题、索引、检查状态在左侧，选中项详情在右侧，SQL / XML / 参数统计 / 改写提案收纳在底部。具体操作与已知限制见本文[第 2 节](#2-执行计划工作区2026-09-11)。下文 IMP 编号保留各轮实施与验收历史，历史测试数量不代表当前版本的验证结果。

节点卡片、浅深色提示框、缩放定位与分支布局已经更新，但工作区尚未完成全部可用性改善。小画布中的大图仍可能缩得不可读；部分文档级、语句级诊断会出现在首个节点的徽标中；空详情及命令命名仍待修正。请结合问题的实际范围判断。当前差距见[设计与评估](DOCS/plan-workspace-design-and-gap-assessment.md)，逐项状态见[执行计划验收矩阵](DOCS/verification/plan-workspace-redesign/acceptance-matrix.md)。离屏截图和自动化通过不代替真实 DPI、实体键盘、屏幕阅读器及原发布流程的人工门槛，M7 未据此关闭。

## IMP-30：候选安装与回退

发布/回退验证脚本需要 PowerShell 7.4 或更新版本。候选与 `BuildEvidence` 必须来自同一次构建；支持相对于调用时目录的输入路径。命令超时或输出捕获不完整会判定失败，需保留阶段清单列出的全部 stdout/stderr，参见[审查修复说明](DOCS/IMP-30审查修复与加固说明.md)。

请保留完整候选目录，通过 `gui/SqlXmlAnalyzer.exe` 或 `cli/SqlXmlAnalyzer.CLI.exe` 启动。先阅读随包发布说明、问题状态和[升级/回退操作单](DOCS/release-materials/UpgradeRollback.md)，备份现有应用、配置、会话与原 SQL，并独立保存清单 SHA-256。恢复操作只创建新目录，不覆盖现有安装和当前 SQL；旧应用使用升级前的旧文件，新格式副本继续保留。

**回退应用不会撤销已执行的数据库 SQL。** 恢复 SQL 文件必须核对对应备份的哈希、编码和目标；数据库变化由 DBA 另行恢复。本轮自动化结果见[证据记录](DOCS/verification/IMP-30-hardening/README.md)。桌面工具拒绝访问导致可见窗口验收待补充，候选尚未批准分发，也未达到 M7 全面验收。

## IMP-29：本轮验收与长报告保存

审查修复新增 25 项验收脚本回归，Debug/Release 全量各 2791 通过。查阅验收结果时，以[当前摘要与构建证明](DOCS/verification/IMP-29/README.md)为准；旧轮次结果保留为历史。源码、程序或测试结果变化后，验收工具会要求重新执行，不能通过刷新指纹沿用旧的通过结论。下文的 2766 为原实施记录。

长报告预览现在在固定区域内滚动；格式选择、“保存为新文件”和关闭按钮不会被正文挤到数千像素以外。紧凑窗口可用外层滚动条到达全部控件。仍请核对预览顶部的语句/事件范围和脱敏选项，再保存为新文件。

本轮验证覆盖多语句/多事件、A/B 与提案审核、配置迁移、报告/诊断包、取消和失败恢复。Debug/Release 全量各 2766 测试通过，但不表示所有真实 DPI、屏幕阅读器或外部报告阅读器均已验收，也不保证大图满足 100 ms P95 候选目标。详见[结果与边界](DOCS/IMP-29完整构建测试与用户场景验收.md)及[逐项矩阵](DOCS/verification/IMP-29/acceptance-matrix.md)。

## IMP-28：打开旧数据与保留原件

旧规则配置可以继续载入，未知规则和扩展字段会保留。显式声明未知 `ConfigurationSchemaVersion` 的配置会被拒绝，当前支持字符串 `"1"`；原文件及当前活动配置保持不变。普通用户无需手工增加版本字段。

配置版本转换已修复大数组、中文等内容被额外格式化后超限的问题。添加版本后真正超过 1 MiB 时仍会报 `CONFIG_MIGRATION_BUDGET_EXCEEDED`，原件保留；可以继续使用旧无版本配置。详见[修复与限制](DOCS/IMP-28审查修复与加固说明.md)。

旧 2.0 或无版本的调优会话在载入后提示另存。保存时请选择新名称，例如 `Tuning_Session-upgraded.pesession`；程序会写出当前 2.1 格式并保留旧文件，已有目标会报错。遇到未知版本、未知扩展或损坏结构，请保留原件并使用匹配版本的程序处理。不要通过删除版本字段强制打开。

CLI 保留原参数及 JSON 外形；`--config` 的默认文件在应用目录，显式相对路径按当前目录解析。`refactor` 成功退出仍可能仅生成候选，需检查 `Outcome` 和 `SourceWritten`；应用改写继续遵循验证和审核流程。报告和诊断 ZIP 用于读取与审核，不能当作会话备份导入。详细[兼容与恢复说明](DOCS/IMP-28模型配置报告与CLI兼容性.md)包含示例和各格式支持范围。

## IMP-27：生成本地诊断数据包

点击左侧包裹图标“本地诊断数据包”，选择要附带的内容，再点“生成 / 更新预览”。清单会列出 ZIP 中每个文件的大小、脱敏状态和 SHA-256；选中文件即可查看实际文本。审核后点“保存审核快照为 ZIP”，使用新的文件名。程序只保存本地，不自动发送。

默认附带可用的脱敏报告，并固定包含版本、采集条件、分析配置、错误与缺失证据。原始选中 XML、日志和 DUMP 默认排除，需分别明确勾选或选文件；日志最多 2 MiB UTF-8，DUMP 最多 64 MiB，二者均无自动脱敏。DUMP 仅能在界面核对大小和哈希，内容需另用本地调试器审核。

预览固定为窗口打开时的语句/事件及生成预览时的附件字节。改变选项后必须重新预览；切换工作区或重新分析后，要收集新结果请重新打开窗口。没有可用报告或报告过期时仍能生成元数据包。详情与限制见 [IMP-27 说明](DOCS/IMP-27可审核的本地诊断数据包.md)。

审核配置时请区分当前内置规则版本与报告快照版本，二者可能不同。缺失证据还包括不完整（Incomplete）及存在歧义（Ambiguous）的指标；取消报告正文后仍保留这些状态。日志出现 UTF-8 提示时，应选择有效 UTF-8 编码的 `.log` 后重新生成预览。

## IMP-26：查看进度、取消与浏览大图

状态栏显示读取、解析、分析、准备视图及完成状态；处理中点击“取消分析”。“部分完成”表示存在可用结果及未完成检查，详情见状态提示和诊断；取消不会显示成功。失败或取消后可重新打开文件。

大型计划图每个分段最多 64 个算子，用“上一组 / 下一组”切换；跨页连线不在当前页绘制，但当前版本的“跨页关系”入口可定位另一端算子。完整算子仍在“热点算子”表，选择诊断、表格行或用图中的 Home/End、方向键可定位其他页。分页不改变诊断和原始证据。打开多事件 XEL 会等待首个事件图完成，选择其他事件后状态重新更新。详见 [IMP-26](DOCS/IMP-26异步状态取消与大图性能.md)；当前计划图操作与可读性限制以第 2 节为准。

准备视图期间可继续切换语句或翻页，程序完成最新选择后再更新完成状态。翻页会定位当前页节点；后台准备期间修改的布局、颜色和连线指标会用于新图。相关修复见[审查修复说明](DOCS/IMP-26审查修复与加固说明.md)。


## IMP-25：布局、主题和键盘操作

当前执行计划工作区按宽度调整侧栏：宽窗口可同时显示两侧，中等宽度在问题与详情侧栏之间切换，窄窗口在图、问题、详情之间切换。其余 IMP-25 布局记录保留为对应轮次的历史。比较页的 F6 / Shift+F6 会跳过不可用动作；没有比较结果时保留唯一可用的历史焦点区。图节点 Enter、双击或辅助技术 Invoke 会展开并聚焦属性栏，在属性区按 Escape 返回原节点。

窗口较窄时计划工作区采用上述面板切换，死锁侧栏自动收起；导航和需要滚动的材料保留滚动入口。使用 F6 / Shift+F6 切换焦点区域，F8 / Shift+F8 选取计划诊断，Ctrl+Shift+G 进入图节点后用方向键定位、Enter 查看详情。Ctrl+Shift+E 查看原始证据，Escape 返回原焦点。Alt+1 / 2 / 3 切换死锁、计划、比较工作区；比较后切回目标计划或死锁，Ctrl+R 打开报告预览。Ctrl+F1 查看完整快捷键并启用“减少动画”，停用自动推演同时保留单步。主题按钮实时更新已打开控件。详见 [IMP-25 使用说明与验证边界](DOCS/IMP-25布局主题与键盘操作.md)。

## IMP-24：配置执行计划规则

审查加固后，应用配置仅取消正在执行的计划分析，死锁/XEL 分析和待识别的文件读取继续执行。重算使用上次成功分析的完整内存快照；如需读取磁盘文件的新内容，请重新打开文件。损坏的 Unicode 配置显示校验错误并保留原草稿，不生成 DUMP。详见[加固说明](DOCS/IMP-24审查修复与加固说明.md)。

点击左侧齿轮“规则配置”，按 ID、类别、范围或说明筛选规则，调整启用与严重度并查看变更预览。“恢复默认草稿”保留未知项；“保存文件”持久化到已载入路径，“应用到本次会话”决定后续分析使用的设置，两者是独立操作。应用后已有结果提示需要重新分析，点击“重新分析当前计划”更新图和诊断列表。

“默认”仍由规则按证据决定严重度。同证据的诊断保留全部命中来源并取最高严重度，因此 RULE_004 / RULE_030 这类重叠来源需分别设置。规则开关不删除 SQL Server 原始警告、采集事实或独立 SQL 改写安全检查。

旧配置、旧 ID 别名和未知规则均可保留；无效配置显示具体属性位置，失败载入保留原草稿。保存检查源是否被其他程序修改并建立备份；若提示已写入或提交结果未知，应重新载入核对。另存只创建新文件；重启固定读取程序目录的 `RuleConfiguration.json`，其他路径不会自动设为启动配置。未知错误生成 DUMP；Debug 输出调试/警告/错误/致命，Release 仅错误/致命。完整契约及截图见 [IMP-24](DOCS/IMP-24兼容规则配置界面.md)。

## IMP-23：搜索 XEL 并追溯事件

死锁工作区点击“XEL 搜索、聚合与追溯…”，可检索当前输入，或点击“添加 XEL 文件…”多选文件。填写时间、数据库、对象或文本后点击“应用筛选”；空白不限制。时间使用 `2026-09-10T08:00:00.1234567+08:00` 或带 Z 的形式，起止时刻均包含；“显示偏移”只改变显示，原始时间始终保留。

首次载入期间“添加”和“应用筛选”暂时禁用；可以取消，随后重新导入。数据库名称包括 XML 中的 `currentdbname`。合法但没有死锁的滚动文件可一起导入，计入来源与未处理记录；损坏文件仍使整次导入失败。超出聚合预算的复杂/大标签事件单独成组，仍能搜索和追溯。

点击左侧聚合组查看成员；“显示全部匹配事件”恢复所有匹配行。选择事件查看源文件、SHA-256、事件/子事件索引、XML 路径和字节位置，点击“回到原始事件”打开对应死锁图及证据。相同 SPID 不足以合并；重复副本全部保留并标记，分组不保证根因相同。

默认最多 32 文件、10,000 记录、128 MiB 输入和 32 Mi 字符，未处理记录明确列出。取消、错误或超限保留上次完整结果；同路径重复添加会跳过。当前输入沿用主窗口快照；文件更新时先关闭检索窗口，在主窗口重新打开文件，再开始检索，额外来源重新添加。快照被修改后，追加和追溯都要求重新载入。详情预览有长度上限，原始事件仍保留。未知错误显示 DUMP 路径或生成失败原因；Debug 记录调试/警告/错误/致命，Release 仅记录错误/致命。完整说明与截图见 [IMP-23](DOCS/IMP-23XEL搜索聚合与事件追溯.md)及[审查加固](DOCS/IMP-23审查修复与加固说明.md)。

## IMP-22：核对报告范围和脱敏结果

选择当前语句/QueryPlan 或死锁事件后，点击 HTML、PDF 或 Word 报告按钮，会先打开统一预览。默认使用脱敏快照，可查看处理类别、数量、未覆盖项和前后样例，再选择 HTML/PDF/Word/JSON 格式保存为新文件。原脱敏计划按钮也使用此预览。未覆盖字段会阻止脱敏保存；取消勾选脱敏则明确进入原始模式。导出期间可取消，已有文件不会覆盖。

报告固定为打开窗口时的选择；切换语句后请重新打开报告。SVG 只有静态图，sqlplan 只有选择范围 XML，完整诊断请用四种报告格式。脱敏自由文本已替换，不能执行其中的 SQL。说明、截图及浏览器本地打开的待验收限制见 [IMP-22](DOCS/IMP-22统一报告与脱敏预览.md)。

遇到 `REPORT_BUDGET_EXCEEDED` 时，请缩小选择范围后重新打开报告。预算在构建和渲染时生效，默认最多 800 万内容字符、10 万条总记录；总记录包含事实字段、诊断和图数据，不等于算子数。全缺失指标显示 Missing/N/A。超限不会输出截断后冒充完整的报告。

游标可分别导出 PopulateQuery 或 FetchQuery，保留语句选项及原始层级。RECEIVE 必须保留两个操作；单操作范围提示 `REPORT_SCOPE_NOT_REPRESENTABLE`，可改用 CLI 文档范围输出完整计划。原始/脱敏游标文件的 XSD 验证及边界见[修复说明](DOCS/IMP-22审查修复与加固说明.md)。

### CLI 扫描的输出与失败状态

`--output` 仅用于 JSON/JUnit，必须指定新文件名；输入文件、硬链接和既有报告不会覆盖。`--max-cost` 只接受有限非负数。JSON 的 `MaxSubtreeCostState` 区分 Available、Missing、Incomplete、Invalid；设置开销阈值时，缺少完整开销不能通过检查。报告超预算返回退出码 1 和明确失败原因，`Plan`、`Diagnostics`、`DiagnosticReport` 为 null；请先检查状态再读取这些字段。

```powershell
dotnet run --project SqlXmlAnalyzer.CLI -- --path capture.sqlplan --format json --output new-report.json --max-cost 10
```

扫描报告属于原始输出，不是脱敏文件。参数错误返回 2，扫描门禁或报告预算失败返回 1；完整行为与样例见 [CLI 加固说明](DOCS/CLI扫描与报告加固-2026-09-09.md)。

## IMP-21：比较、改写和索引审核

审查加固后，复制 SQL 或保存新文件的成功提示明确标注“未脱敏”；输出保留完整 SQL，不应作为脱敏材料共享。原文件是否已写回，以窗口应用状态为准；导出不改变该状态，也不清除应用失败的诊断信息。索引审核发生未知错误时会清空旧脚本、禁用复制并显示 DUMP 结果，故障修复后可重新生成。详见[修复说明](DOCS/IMP-21审查修复与加固说明.md)。

在 A/B 工作区捕获计划，选中历史快照后点击“设为 A（基准）”或“设为 B（待比较）”。匹配摘要列出配对、待确认及两侧未匹配数量；展开清单和可比性可核对语句位置、SQL Server Build、CE、参数和运行采集条件。没有运行证据时保持 N/A；B 仅表示待比较版本。

SQL 审核窗口的“逐项选择与差异”页展示每项前提、风险、证据及替换片段；“完整 SQL 对照”查看最终所选组合。“复制候选 SQL”和“保存为新 .sql 文件”仅输出候选，保存不能覆盖已有文件；修改选择后需要重新导出。原文件仍须先“验证所选 SQL”、审核场景，再“备份并应用到原文件”。

索引审核顶部显示实例、数据库、架构和表，未知部分显示 `?`。核对键顺序、INCLUDE，并按目标环境调整 ONLINE、SORT_IN_TEMPDB、压缩与 MAXDOP；名称留空使用自动名称，自定义时填写原始单个名称。创建和回滚脚本使用同一名称及目标；无效选项会清除旧脚本。窗口不连接服务器核验版本、既有索引或空间，也不执行 DDL；原始 Impact 仍为优化器估算，工具评分和沙盒假设不代表性能收益。

未知异常提示可复制的 DUMP 信息；生成失败会明确显示原因。Debug 记录 DEBUG/WARN/ERROR/CRITICAL，Release 仅 ERROR/CRITICAL。操作、截图及验收记录见 [IMP-21](DOCS/IMP-21审核界面.md)。

## IMP-20：选择语句 / 事件并核对证据

计划工作区顶部可选择 Batch 和 Statement / QueryPlan。SQL、图、树、热点算子表、缺失索引和问题列表同时切换；没有采集算子的语句仍可查看原 SQL，并显示空态说明。当前版本将命中诊断放在“问题”页，将 Skipped / Failed 等检查记录放在“检查状态”页，避免混排；完整运行结果仍保留。

点击问题定位对应节点，再从“证据”下拉框选择事实。点击“查看原始 SQL/XML”核对完整身份、原始路径、行列与 XML 片段。NodeId 相同的不同语句不会混淆；文档级诊断中的证据可以跳转至其他语句。复制当前诊断仍包含完整证据。

死锁工作区使用事件选择器同步进程、资源、图、诊断和推演；在“证据 SQL/XML”选择证据，会高亮对应进程/资源并显示原始位置。快速切换只保留最后一次选择。依赖推演表达快照中的等待关系，不代表真实时间回放。

清空结果会取消正在进行的分析并清除事件选择器。未知异常会记录致命日志并生成 DUMP，失败时明确显示原因；Debug 记录调试/警告/错误/致命信息，Release 仅错误/致命信息。操作样例、截图和已知边界见 [IMP-20](DOCS/IMP-20语句事件选择与诊断证据.md)。

手动选择死锁进程或资源时，证据下拉框会清除选中；重新选择原证据可恢复其原文与高亮。定位折叠中的计划节点只展开必要祖先，保留其他分支状态。工作区“SQL 改写阶段（文档分析结果）”保留失败、跳过和 DUMP 提示，复制诊断也包含这些信息。详见[审查加固说明](DOCS/IMP-20审查修复与加固说明.md)。

## IMP-19：验证所选 SQL 并应用

联合审查更新：SQL 文件超过 256 Ki 字符时会在改写前拒绝；结果中含 `sql_variant` 时返回 `UnsupportedSemanticType`，目前不能用于应用验证。窗口顶部直接显示已写回、未写回或提交结果未知，并给出目标/备份信息；结果未知时先核对当前文件和备份，再决定恢复操作。详细限制和证据见 [联合审查修复](DOCS/IMP-18-19联合审查修复与加固说明.md)。

场景中的 `ObserveSql` 现在只允许只读 SELECT；清理、UPDATE、DELETE、SELECT INTO、赋值或事务语句会报 `ObserverReadOnly`，验证器负责数据库清理。读取 `@@ROWCOUNT` / `@@ERROR` 时放在观察器第一条查询中，后续语句按 SQL Server 自身顺序改变这些瞬时值。报告的 `RecordsAffected=-1` 表示没有适用的 DML 计数，不是负一行更新；0 表示零行 DML。迁移和反例验证见 [IMP-19 审查修复](DOCS/IMP-19审查修复与加固说明.md)。

先准备专用 SQL Server 2019/2025 LocalDB 与包含代表性数据的场景 JSON。在审核窗口选择提案，点击“验证所选 SQL”，选择与左侧原文完全一致的 SQL 文件及场景文件。查看结果、错误、事务和对象观测；只有验证成功后才能勾选场景审核确认，再点击“备份并应用”。选择改变需重新验证，关闭窗口会取消执行；源文件已变化、备份失败或清理失败都会阻止应用。保存内容是右侧已选预览，结果给出备份及实际写入状态。

CLI 新增 `rewrite-validate` 生成验证报告，`rewrite-apply` 要求同一提案选择、源/预览/场景三份审核 hash 及 `--acknowledge-scenarios`，并在写回前重新验证。`semantic-compare` 只比较两份 SQL，不写源文件。`PassedForScenarios` 仅表示所列场景成立，不证明任意 SQL 等价或性能改善；高风险 TRIM/表变量规则继续受限。报告及 DUMP 未脱敏。安装命令、可复制示例、比较限制、日志路径、取消及恢复说明见 [IMP-19 使用与验收](DOCS/IMP-19SQL语义验证与可靠应用.md)。

## IMP-18：审核 SQL 改写提案

在“SQL 改写提案”页点击“审核提案”，或点击标量子查询灯泡，查看规则版本、源 hash、SQL diff、前提、风险与证据。候选默认未选择，右侧保留原文；选择后续项会同时包含前序依赖，取消前项会取消后续依赖，取消全部选择恢复原文。单独选择或关闭窗口不触发写回；应用需完成上述 IMP-19 验证和审核流程。

CLI `refactor query.sql --format json --show-sql` 生成提案；重复使用 `--select <提案ID>` 可组合预览。**不传 `--dry-run` 也不会写回文件。** 语法有效不等于语义等价，静态提案的 `CanApply=false`；数据库验证与应用使用上述 IMP-19 独立流程。

`Review.PreviewSql` 是所选预览，旧 `RefactoredSql` 仅表示完整候选。报告未脱敏。Debug 日志包含调试/警告/错误/致命，Release 只输出错误/致命；业务风险说明在两种配置下都保留。未知异常生成 DUMP，默认位于 `%LOCALAPPDATA%\SqlXmlAnalyzer\dumps`。详见 [契约、CLI 示例与验证边界](DOCS/IMP-18可审核SQL改写提案.md)。

文本报告及 `.txt` 导出的末尾 `Selected review preview (not applied)` 与 JSON `Review.PreviewSql` 一致；没有选择时保留原始 SQL。`--show-sql` 展开原文、各候选 diff 和已选预览，未选择的候选仍可在清单中审核。新增、删除或改变 SELECT INTO 目标会被静态检查拒绝；通过检查仍不代表可应用。见 [审查修复说明](DOCS/IMP-18审查修复与加固说明.md)。

## IMP-17：比较全部语句或人工选择

当前比较模型 1.2.0：残差条件、排序列/方向/去重属性不同，或必要证据缺失时，相关算子差值为 N/A。访问对象缺少数据库、schema 或对象名时，整个 QueryPlan 不生成数值差值；人工选择双方也不能补足这些证据。本侧观测值继续保留，详情给出原因。详见[证据完整性说明](DOCS/IMP-17比较证据完整性加固.md)。

在调优历史中设置计划 A/B 后，比较页默认显示全部语句的匹配、待确认与未匹配项。可用“人工选择 A/B 的语句”下拉框选择任意 QueryPlan，点击“比较所选”；“全部语句”恢复自动匹配。B 表示待比较计划。

展开“匹配依据与可比性”或悬停树节点，查看置信度、版本、参数编译/运行值和指标来源。两侧缺少记录、采集条件不同或执行口径无法确认时，值仍保留，差值为 N/A。零基准只计算绝对差值；B 树显示 B−A，A 树反向。估算成本不是耗时，也不证明性能改善。会话保存包含人工选择，旧会话可继续读取。详见 [示例、截图与限制](DOCS/IMP-17多语句与可比性检查的AB比较.md)。

审查修复后，同一份快照也可在 A/B 选择不同语句；交换计划会交换各侧选择，保存/载入会话后保留，“全部语句”会清除两侧选择。Seek 的列或范围条件不同、结构证据不完整时，相关算子不输出数值差值。详情中的“原始文件字节（会话记录，未重新读取原文件验证）”表示保存的来源记录；若标为“与当前 XML 的关联未核验”，当前指纹使用 XML 表示，历史原始指纹仅供追溯。新格式会话的内容与保存的 XML 指纹不一致时会明确报错。详见 [审查修复说明](DOCS/IMP-17审查修复与加固说明.md)。

## IMP-16：阅读索引评分与模拟结果

沙盒将 SQL Server 原始 Impact、工具规则评分和输入假设分开展示。Impact 是优化器估算；没有捕获该值时显示 N/A。工具评分可查看等值、范围、排序、已知覆盖和宽度惩罚各项贡献，缺少覆盖证据不再默认加分。谓词文本的语法分析会注明较低可信度。

审查修复后，括号和带正负号的数值常量正确计分，IS NULL/IS NOT NULL 分别按等值/范围计分；CASE 内部条件不再增加分数。评分模型版本为 2.0.1，既有分数和排序可能变化。Convert/ConstExpr 等计算表达式仍可能显示“未识别到可评分的谓词证据”，这不表示查询不能使用索引。详见 [修复、真实计划和限制](DOCS/IMP-16审查修复与加固说明.md)。

成本面板显示捕获范围与关联访问算子成本，先计算完整总量，多个候选关联同一算子只计一次。成本占比不是节省比例，收益预测仍为 N/A；不能据此保证 Seek、消除 Lookup/Sort 或实际耗时下降。

总行数、行宽和返回行数取当前目标/QueryPlan 的计划估算最大值，标为假设；缺失时明确显示工具默认，修改后显示“用户编辑”。可在右侧面板滚动查看模型版本、权重和输入来源。部署前仍需在相应环境核验实际计划与整个工作负载。详见 [IMP-16 说明](DOCS/IMP-16模拟顺序依赖与模型限制.md)。

## IMP-15：核对索引目标与脚本

沙盒只列出计划中能确认目标归属的实体列，参数及内部表达式不会成为可选索引列。名称看起来像参数的真实列仍可使用；实体列 `[literal]`、`R]ange` 保留原义。排序评分只采用当前 QueryPlan 的完整对象/列/方向证据，分数仍为待验证的工具假设。详见 [审查加固说明](DOCS/IMP-15审查修复与加固说明.md)。

索引建议会保留数据库、schema、表和所属语句。`[Order.Detail]` 是一个名称；右方括号、空格和中文正确引用。缺少 schema 或 SQL 候选无法唯一关联时不生成 DDL。已提供服务器名时，脚本会先校验服务器；数据库未知时会提示核验执行上下文。

新索引名使用完整定义摘要，创建和回滚名称一致；旧版已部署索引请使用原部署时保存的回滚脚本。部署前仍需核对既有索引、列类型、权限及 ONLINE/压缩选项适用条件。RULE035 按语句显示全部候选，不把同名表或 NodeId=0 混为一个目标。说明及 LocalDB 实测见 [IMP-15](DOCS/IMP-15索引目标身份与DDL标识符.md)。

## IMP-14：查看基数与残差证据

独立或游离 XML 算子的诊断、JSON 和失败结果现在保留原 NodeId；没有完整计划身份时，文本显示“局部 NodeId”。相同编号可能出现在不同语句，请结合完整位置定位。见 [审查修复说明](DOCS/IMP-14审查修复与加固说明.md)。

基数提示现在同时列出估算单次行数、实际总输出、逻辑执行次数、实际单次行数及比较分母。并行 worker 次数不会直接相加；分母最低按 1 行计算，零值或小数估算时提示会解释这个限制。统计信息、参数和过滤条件属于待验证原因。

残差提示区分“存在残差”和“实测读取放大”；Scan 与 Seek 均可显示读取/输出差值。未采集完整计数时，读取放大规则标为缺少证据；计数矛盾时也会跳过。输出为 0 时倍数显示 N/A。相同节点上重复命中的相关规则合并成一条提示，详情中的 Origins 保留全部来源，不同语句的相同 NodeId 仍分开。

原有规则配置继续有效。RULE006 确认读取放大时为 Warning，RULE034 新增 Scan 支持，因此告警数量或等级可能变化。未知异常保留 Failed 和 DUMP 结果；Debug 记录调试/警告/错误/致命，Release 仅错误/致命。说明、阈值与 CLI 样例见 [IMP-14](DOCS/IMP-14基数与残差谓词规则修复.md)。

联合审查修复后，合法空计划在读取、CLI 与默认诊断中具有相同能力标记；扩展内容中的同名算子不会被当作运行证据。平行死锁关系的箭头沿节点边界连接，清空结果会一并释放原始输入快照引用。详见 [IMP-09 至 IMP-13 修复说明与验证](DOCS/IMP-09至13联合审查修复与加固说明.md)。

## IMP-13：查看共享死锁环与证据

主图和依赖推演现在共享环成员，支持重叠环和多个受害者。图中环成员、关系提示及资源详情可追溯到同一份数据；Exchange/SyncPoint 的原始 ID 和属性在资源详情中保留。退出推演后仍显示全部受害者。

同一进程与资源之间的不同关系分别显示推演步骤，拖动节点后箭头和步骤标记一起移动。Mermaid 导出保留索引名称。范围锁证据来自实际 mode 字段；缺失进程的原始字段仍可查看，但不表示已确认等待依赖。详见[审查修复说明](DOCS/IMP-13审查修复与加固说明.md)。

“解释环路径已截断”表示路径数量、长度或搜索步数达到预算；环成员仍由完整等待图确定，不代表没有其他环。默认最多显示 64 条解释路径、长度 128、搜索 100,000 步。推演是捕获关系的合成解释，不是真实事件时间线。

诊断详情和报告新增规则编号、置信度、来源与证据。范围锁必须有实际 Range 模式；回表、锁转换、热点、级联和并行原因均需进一步验证。每条诊断最多展示 64 项证据，超出会说明。未知错误自动生成 DUMP；Debug 记录调试/警告/错误/致命，Release 仅错误/致命。完整说明和示例见 [IMP-13](DOCS/IMP-13统一死锁图环与诊断依据.md)。

## IMP-12：查看证据与规则运行状态

诊断面板和节点提示现在显示规则版本、完整位置、证据及置信度，并把事实、假设、建议与限制分开。Hit 表示命中，NoHit 表示已执行但未触发；Skipped 显示禁用、不适用或缺证据原因，Failed 表示规则执行失败。没有命中不代表计划完全健康。

节点提示的“诊断运行状态”独立展示调用计数和跳过原因；仅有 NoHit 或 Skipped 不会点亮性能警告图标。明确串行的算子无需提供并行 worker 分布，线程倾斜规则显示“不适用”；真正缺少并行计数仍显示“缺少证据”。

出现 Failed 时，查看对应 RuleId、原因和 DUMP 结果；自动重构会停止。未知错误会尝试生成真实 DUMP 与异常侧车文件，若生成失败，报告会保留原错误和捕获失败说明。Debug 记录调试、警告、错误、致命信息，Release 仅记录错误和致命信息。

CLI JSON 的每个扫描结果新增 `Diagnostics`，其中 `Runs` 是全部调用记录，`Diagnostics` 是去重后的诊断；JUnit 的 system-out 同样包含证据和状态。scan 的 Passed/退出 0 只表示当前门禁未失败，仍可能缺少运行证据；要求实际数据的 CI 可额外检查 `Diagnostics.HasMissingEvidence`。原 RuleId、配置和阈值继续有效。详见 [IMP-12 协议、迁移与示例](DOCS/IMP-12有证据和运行状态的诊断协议.md)。

低效并行等旧提示继续使用原结果编号。例如结果 `RULE_010_INEFFECTIVE_PARALLELISM` 的执行来源仍为 `RULE_009_PARALLEL_SKEW`，启用和严重度配置请使用执行来源编号。GUI/CLI 同一输入的能力标记保持一致。完整映射及样例见[审查修复说明](DOCS/IMP-12审查修复与加固说明.md)。

**指标口径更新（IMP-11）：** 图和表格的实际行数是当前算子全部线程/执行的总输出，读取行另列；缺失或非法值显示 N/A。属性面板、连线提示同时提供线程执行次数合计与可确定的逻辑执行次数。基数偏差按每次执行比较，16 个 worker 各执行一次不会被当成循环 16 次。线程计数不完整或执行次数存在歧义时不显示偏差倍数。

合法的 `Thread="+0"`、`Thread="+01"` 等编号现与普通十进制编号一致，不会因前导符号丢失告警。等价编号重复、编号非法或角色不明确时，仍显示未知或停止相应比较，原始计数按完整性规则保留。

父算子的对象、谓词和分区信息不再混入子节点内容。自身成本只在当前及直接子节点成本完整时计算；无表对象的 Scan 不推断为堆表。CLI read/scan 的算子 Facts 同时提供值、单位、来源、状态、聚合方式和线程原始值。日志/DUMP 的路径与分模式策略沿用既有配置；详见 [IMP-11 说明与示例](DOCS/IMP-11集中算子事实与指标口径.md)。

欢迎使用 **SqlXmlAnalyzer**。它旨在成为每一位 DBA、后端开发人员和架构师在调优 SQL Server 查询时最得力的助手。

## 1. 打开文件 📂

SqlXmlAnalyzer 支持以下 SQL Server 诊断文件：
*   **`.sqlplan`**: 包含查询语句的真实/预估执行计划。
*   **`.xdl`**: 包含死锁信息的 XML 死锁图谱。
*   **`.xml`**: 按实际内容识别计划或死锁包装。
*   **`.xel`**: 包含扩展事件的二进制文件，实际解码并检查死锁载荷。

**操作方式：**
1.  在主界面点击左上角的 **"打开 SQL 执行计划或死锁文件..."** 按钮。
2.  直接从资源管理器中 **拖拽 (Drag & Drop)** 文件到软件窗口内即可极速加载！

**输入识别（IMP-06）：** XML 按内容判定，扩展名只是入口提示；`.xml` 可包含计划或死锁，改名不会使无效 XML 变成有效计划。ShowPlan 根必须使用标准命名空间和 BatchSequence / Batch / Statements 结构；合法的无 RelOp 语句可以打开。

死锁 XML 支持裸 `deadlock`、`deadlock-list` 和 XE `event/data/value` 包装，以及 `events` / `RingBufferTarget` 集合。多事件文件显示下拉框，默认展示第一条有效事件；标签保留源序号和已有时间。IMP-09 起，完整 XML/XEL 集合中有有效及无效记录时返回 **Partial**：显示未处理记录的位置和原因，仍可选择有效事件，悬停选择器可再次查看范围。整体 XML 语法损坏、XEL 文件损坏、超限或取消时不提交文档。

切换死锁事件后，刷新仍读取原文件，重新默认选择第一条。计划文件的编码由 XML 字节流识别，GUI、scan 与 refactor 辅助计划入口使用同一读取规则；兼容有效 BOM 与声明不一致的旧文件，但损坏字节会明确失败，不会自动替换字符。

打开失败会显示稳定的 `INPUT_*` 代码和原因。CLI scan 返回 `Failed` 和退出码 1；refactor 的辅助计划失败时不生成候选、不写回源 SQL。JSON 输出新增 `InputStatus`、`InputKind`、`InputErrorCode`，诊断日志写入 stderr 和日志文件。

IMP-09 审查修复后，空 XE 死锁包装也会显示未处理位置，后续有效事件不会重新编号；合法的长 XML 编码声明可按原编码读取。独立 CLI 的 read、scan、redact、refactor 共享 Ctrl+C 取消令牌，在协作检查点停止并返回 130，重构取消后不另写失败报告。详见[加固说明](DOCS/IMP-09审查修复与加固说明.md)。

**读取预算与能力（IMP-09）：** 默认输入最多 64 MiB、XML 字符 33554432、深度 128、节点 1000000、XEL 事件 10000。超限显示 `TooLarge / INPUT_TOO_LARGE`，取消显示 `Cancelled / INPUT_CANCELLED`，不冒充“分析成功”。能力只说明已观察到语句、算子、运行计数或死锁，不表示完整观测。文件哈希来自同一原始快照，缺失时间/版本保持未知。

独立 CLI 可用 `read capture.xel` 查看来源、能力、状态和事件位置 JSON；用 `--read-options limits.json` 调整 scan/read/refactor 辅助计划的预算。Success 退出 0，Partial/读取失败退出 1，选项错误退出 2，取消退出 130。批量扫描单文件失败后继续处理。配置示例和测试边界见 [IMP-09 使用与契约说明](DOCS/IMP-09统一文档读取与能力契约.md)。

**计划归属（IMP-10）：** 多语句文件保留全部 SQL，文本和 Mermaid 图标明批次、语句、QueryPlan；诊断位置包含文档身份。同名 NodeId 的图形选择互不混淆。对象标签分别显示服务器、数据库、schema、对象名，`?` 表示未知，别名独立显示。缺失索引关联要求名称及来源精确匹配，有歧义时不自动选第一个候选。

多语句计划在没有明确选择单条语句时暂停自动重构并显示原因。A/B 比较自 IMP-17 起默认覆盖全部语句，两侧下拉框分别用于显式缩小/人工配对范围，选择互不覆盖。独立 CLI 的 `read plan.sqlplan`、scan JSON 已包含完整 Plan 层级、算子与对象位置，属于未脱敏输出。身份契约见 [IMP-10 说明](DOCS/IMP-10语句算子与对象身份.md)。

比较选择错误现在显示在两侧树中，交换、载入会话及清空操作仍会完成；恢复有效选择后可重新比较。全局索引汇总只标文档范围，不指向某个表算子。无效 NodeId 显示 `PLAN_NODE_ID_INVALID`，算子仍按源顺序保留。CLI read 在身份构建阶段收到取消时返回 130，不输出成功 JSON。详见[IMP-10 审查修复](DOCS/IMP-10审查修复与加固说明.md)。

Debug 记录 DEBUG / WARN / ERROR / CRITICAL；Release 只记录 ERROR / CRITICAL。未知错误自动尝试生成 Windows DUMP 和异常记录，正常输入错误、权限/读取错误、用户取消不生成 DUMP。默认日志位于 `%LOCALAPPDATA%\SqlXmlAnalyzer\log`，DUMP 位于 `%LOCALAPPDATA%\SqlXmlAnalyzer\dumps`，失败时回退 `%TEMP%\SqlXmlAnalyzer\dumps`；无法生成时明确报告失败。完整范围与限制见 [IMP-06 说明](DOCS/IMP-06统一最低输入识别说明.md)。

---

## 2. 执行计划工作区（2026-09-11）

### 2.1 选择范围和查看材料

打开 `.sqlplan` 后，先核对顶部文件摘要、计划数据类型、批次和语句 / QueryPlan。问题数量与检查状态显示在摘要中。改变规则配置后，如果结果需要更新，使用“重新分析”；重新分析使用当前已加载快照，磁盘文件已经改变时应重新打开文件。

中央有“计划图 / 热点算子”两个页签。“问题”与“详情”按钮控制侧栏，宽窗口可同时显示两侧，窄窗口会在图、问题、详情之间切换。侧栏宽度、显示模式和热点表的列配置会保存。当前“布局”按钮只打开恢复默认布局与显示列的菜单；调整图的水平 / 垂直布局应进入图工具栏的“显示设置”。

完整 SQL 位于底部默认收起的“SQL · XML · 参数统计 · 改写提案”区域。展开后选择相应页签；在诊断详情点击“查看原始 SQL / XML”可直接进入证据原始位置。SQL 和 XML 使用可滚动文本区域，当前指南不承诺语法高亮或无限长度显示。

### 2.2 选择节点、问题与热点

- **单击节点**：选中算子并更新详情，左侧完整问题列表继续保留。节点卡显示算子名称、对象简称、主指标及诊断徽标；蓝色边框表示选中，红 / 橙徽标表达诊断严重度。
- **双击节点或按 Enter**：打开“全部属性”并聚焦属性区。在属性区按 Escape 可返回原节点。
- **单击问题**：按完整语句 / QueryPlan / 算子身份定位，并显示诊断证据；折叠或跨页算子会通过导航切换。不同语句中同名 NodeId 不视为同一个算子。
- **使用热点算子**：单击行同步选择，按列排序比较成本、实际行、估算行、偏差和问题数；“显示列”可选择额外字段，双击行或按 Enter 打开该节点属性。

左侧“问题”只显示命中诊断，可搜索和按严重度筛选；“索引”单列索引建议；“检查状态”保留跳过和失败记录。检查失败表示某项分析没有完成，不能直接当作 SQL 发生严重性能故障。

### 2.3 缩放、折叠与跨页导航

| 操作 | 当前行为 |
| --- | --- |
| 适应图 | 缩放并居中当前可见分段，折叠后隐藏的节点不参与适配 |
| 100% | 恢复原始显示尺寸，保留当前视图中心 |
| `+` / `−`、鼠标滚轮 | 放大 / 缩小计划图；底部显示当前缩放比例 |
| 定位选中 | 返回选中算子，必要时完成跨页定位 |
| 查找 | 搜索算子、对象或 Node ID；按 Enter / 查找继续下一个匹配，包含当前页外和折叠中的节点 |
| 节点内的 `+` / `−` | 展开 / 折叠该节点子树 |
| 显示设置 | 水平 / 垂直布局、成本强度、连线指标、连线数值、全部展开及低成本无警告子树折叠 |

可在画布空白处拖动平移。普通连线不持续播放动画；远景隐藏次要文字与数值，保留图结构、选中状态和问题标记。折叠低成本无警告子树只是减少当前显示量，不构成“该分支没有性能问题”的结论。

每个分段最多显示 64 个算子。使用“上一组 / 下一组”翻页；“跨页关系”列出当前分段与其他分段之间的连接目标，可继续定位另一端。图底部显示范围和折叠数量。分页边缘没有画出的连线，不表示执行计划到此结束；完整算子仍可从热点表或查找进入。

节点提示框沿用当前浅色 / 深色主题，浅色使用浅底，深色使用较亮的灰蓝底。提示框只提供摘要；完整对象名称、谓词、输出列与原始属性请进入详情或“全部属性”。

### 2.4 指标口径与缺失状态

没有手工指定图指标时，存在可比运行行数的计划优先显示“实际 / 估算行数”，否则显示估算成本；手工选择会作为显示偏好保留。行数对比使用**每次执行**口径，倍数为实际 ÷ 估算；实际输出总行数和实际读取总行数是独立字段。

缺少实际执行次数、部分计数不完整或其他可比条件不满足时，比较值保持 `N/A`，即使实际总输出行已经可用。真实零值保留为 0；估算为 0 时使用“估算为 0”或“均为 0”，不生成无穷大的倍数。请以当前字段标签区分总计、每次执行和线程最大值，不能直接相加为总耗时。

估算成本是优化器成本单位，不是实测耗时。当前总成本占比采用自身估算成本除以当前计划中最大的估算子树成本；CPU / I/O 强度相对于当前计划中对应单算子的最大估算值，提示框会说明口径。“工具重估成本”是工具按行数比例得到的指标，不代表 SQL Server 实测成本或性能收益。执行模式、Parallel / Ordered 等字段缺失时仍显示未知状态，不按算子名称推断。

索引页的“SQL Server 估算影响”来自捕获的缺失索引建议；“工具优先级评分”仅用于排序；语法推导建议没有实测收益。沙盒可调整键列、包含列与脚本，但模型假设、回表参考区间和工具评分都需要在目标环境验证。

### 2.5 参数与统计信息

打开底部“参数统计”时先查看来源状态。**尚未手工载入直方图时，图表默认属于概念性教学模拟**；没有当前选择的参数时显示未采集。编译参数和运行时参数不同不等于已经证实参数嗅探问题，模拟曲线也不是从当前服务器采集的真实数据分布。

需要真实直方图时，在 SSMS 中核对并执行控件提供的 `DBCC SHOW_STATISTICS` 命令，将直方图网格连同列头复制为 Tab 分隔文本，粘贴到输入框，再点击“载入并绘制真实直方图”。未使用 `WITH HISTOGRAM` 时通常是第三个结果集；输入应包含 `RANGE_HI_KEY`、`RANGE_ROWS` 等列。工具支持手工载入，不会自动连接 SQL Server 采集。

手工直方图中的 EQ_ROWS / AVG_RANGE_ROWS 说明性估算不能替代 SQL Server 基数估算，也不能证明查询性能收益。用户需核对统计对象、数据库、采集时间与当前计划是否对应。点击“清除并恢复模拟”会恢复教学显示；切换并重新加载参数也会清除先前手工载入的数据。

### 2.6 当前已知限制

- **小画布中的大图可能不可读。** 工具栏和分页区换行后会挤压画布，“适应图”可能把全部节点缩到肉眼难辨。暂可扩大窗口、收起侧栏，使用查找 / 热点表定位，再恢复 100% 查看局部；可读默认视图仍待 PW-01 实施。
- **节点徽标的诊断范围仍待修正。** 部分文档级、语句级或 QueryPlan 级诊断目前会计入该范围首个算子的徽标。请查看问题列表和详情中的实际范围，不要仅凭徽标认定该算子发生故障；对应 PW-02。
- **问题摘要和详情仍有阅读负担。** 索引问题摘要可能是截断 DDL；没有问题的节点仍会显示空的原因、建议和原始证据区域；内部单位和部分原始值也可能直接显示。对应 PW-03 / PW-04。
- **命令组织仍待改善。** “计划图”按钮与页签重复，“问题 / 详情”按钮的开关状态不够明显，“布局”仅用于恢复默认。当前操作按上表说明，后续由 PW-05 调整。

上述问题已经登记，尚未因文档更新而修复或关闭。当前截图为生产 WPF 离屏渲染，几何适配通过不等于实际可读性、真实显示器 DPI、实体键盘或屏幕阅读器验收通过。后续工作见[实施规划](DOCS/软件改善实施规划.md)，具体状态见[执行计划验收矩阵](DOCS/verification/plan-workspace-redesign/acceptance-matrix.md)。

---

## 3. 死锁分析器 (Deadlock View) 💀

若您加载的是 `.xdl` 文件，界面将自动切换至死锁分析模式：
*   **资源矩阵与死锁图**：展示进程与独立物理锁资源之间的 `Request` (请求) 与 `Owner` (持有) 关系。
*   **依赖推演**：点击“自动推演”或选择步骤，查看快照中持有/等待关系的合成解释。固定说明始终保留：这不是锁事件真实发生顺序，播放间隔也不是事件耗时。
*   **观测与假设**：范围锁条目要求实际 Range 模式并列出证据，不能仅凭 REPEATABLE READ 或 SERIALIZABLE 判断根因。其他启发式诊断须结合完整等待环验证。

**A/B 比较（IMP-08，IMP-17 已扩展）：** 缺失、无效或部分线程不完整的运行指标显示 N/A，真实 0 保留。当前按语句/对象/结构证据对照，并校验采集与执行口径；顶部不再用总成本给出改善率。详见 [IMP-17 契约及验证](DOCS/IMP-17多语句与可比性检查的AB比较.md)。

依赖推演及沙盒的模型限制、N/A 和假设状态文字会随浅色/深色主题更新，在已打开窗口中切换主题即可生效。见[主题修复与验证](DOCS/IMP-08审查修复与加固说明.md)。
    *   **步骤状态气泡**：指示器以不同颜色反映已播放、未播放、当前执行帧。
    *   **受害者 (Victim) 标记**：死锁中最终被杀死的受害者节点（气泡）会用显眼的骷髅 **💀 标识与红色发光阴影** 进行警示。
    *   **现代交互操作栏**：提供重置、上一步、播放/暂停、下一步、聚焦关键环以及播放速度调整（慢/中/快）等多功能栏。
*   **🎯 聚焦关键环**：勾选“聚焦关键环”，工具会自动利用 DFS 算法过滤掉其他未参与死锁链条的噪音节点。

## 4. 快捷功能与导出 📤

在中心画布顶部的工具栏中：
*   **【导出为图片】**：支持将当前调整好视角、放大比例完美的执行计划树，一键导出为高清 `.png` 截图，直接用于您的技术报告或 PPT 汇报。
*   **【复制 Mermaid 代码】/【在浏览器查看图形】**：一键将庞大计划树转化为标准 Mermaid JS 流程图文本，方便粘贴入您的 GitHub README 或是 Wiki 文档。

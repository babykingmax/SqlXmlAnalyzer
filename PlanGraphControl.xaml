<UserControl x:Class="SqlXmlAnalyzer.PlanGraphControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:nodify="clr-namespace:Nodify;assembly=Nodify"
             xmlns:local="clr-namespace:SqlXmlAnalyzer"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <UserControl.Resources>
        <local:ConnectionThicknessConverter x:Key="ConnectionThicknessConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <!-- 纯图形区域 (Plan Explorer 风格操作符盒子图) - 无内部侧边栏，避免布局冲突 -->
        <Border BorderBrush="#90A4AE" BorderThickness="1.5" Background="#F5F7FA" CornerRadius="4">
            <Grid>
                <!-- 顶部工具提示栏 -->
                <Border Background="#ECEFF1" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Height="36" VerticalAlignment="Top" Panel.ZIndex="10">
                    <StackPanel Orientation="Horizontal" Margin="8,0">
                        <TextBlock Text="📊 执行计划节点图" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center" Foreground="#37474F"/>
                        
                        <!-- 视图选项：动态切换显示模式 -->
                        <TextBlock Text="视图模式:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="#546E7A"/>
                        <ComboBox x:Name="CmbViewMode" SelectedIndex="0" Width="120" Height="24" SelectionChanged="CmbViewMode_SelectionChanged" VerticalContentAlignment="Center" FontSize="11">
                            <ComboBoxItem Content="成本百分比 (%)"/>
                            <ComboBoxItem Content="CPU + I/O 成本"/>
                            <ComboBoxItem Content="行数对比 (Act/Est)"/>
                        </ComboBox>

                        <TextBlock Text="布局方向:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="#546E7A"/>
                        <ComboBox x:Name="CmbLayoutMode" SelectedIndex="0" Width="80" Height="24" SelectionChanged="CmbLayoutMode_SelectionChanged" VerticalContentAlignment="Center" FontSize="11">
                            <ComboBoxItem Content="水平 (H)"/>
                            <ComboBoxItem Content="垂直 (V)"/>
                        </ComboBox>

                        <TextBlock Text="热力图模式:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="#546E7A"/>
                        <ComboBox x:Name="CmbColorMode" SelectedIndex="0" Width="110" Height="24" SelectionChanged="CmbColorMode_SelectionChanged" VerticalContentAlignment="Center" FontSize="11">
                            <ComboBoxItem Content="总成本百分比"/>
                            <ComboBoxItem Content="CPU 成本"/>
                            <ComboBoxItem Content="I/O 成本"/>
                        </ComboBox>

                        <TextBlock Text="连线粗细:" VerticalAlignment="Center" Margin="12,0,4,0" FontSize="11" Foreground="#546E7A"/>
                        <ComboBox x:Name="CmbLinkMetric" SelectedIndex="0" Width="100" Height="24" SelectionChanged="CmbLinkMetric_SelectionChanged" VerticalContentAlignment="Center" FontSize="11">
                            <ComboBoxItem Content="数据行数"/>
                            <ComboBoxItem Content="数据大小"/>
                        </ComboBox>

                        <Button Content="全部展开" FontSize="9" Padding="6,1" Margin="12,0,0,0" Click="ExpandAll_Click" VerticalAlignment="Center"/>
                        <Button Content="智能折叠(&lt;5%)" FontSize="9" Padding="6,1" Margin="6,0,0,0" Click="SmartCollapse_Click" VerticalAlignment="Center" ToolTip="折叠子树成本小于5%且无警告的节点"/>

                        <Button Content="重置视图" FontSize="9" Padding="6,1" Margin="6,0,0,0" Click="ResetView_Click" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <nodify:NodifyEditor x:Name="Editor"
                                     VirtualizingPanel.IsVirtualizing="False"
                                     ItemsSource="{Binding Nodes}"
                                     Connections="{Binding Connections}"
                                     Background="Transparent"
                                     Margin="0,26,0,0"
                                     SnapsToDevicePixels="False" 
                                     UseLayoutRounding="False"
                                     TextOptions.TextRenderingMode="ClearType"
                                     TextOptions.TextFormattingMode="Ideal"
                                     TextOptions.TextHintingMode="Fixed"
                                     PreviewMouseLeftButtonDown="Editor_PreviewMouseLeftButtonDown"
                                     PreviewMouseMove="Editor_PreviewMouseMove"
                                     PreviewMouseLeftButtonUp="Editor_PreviewMouseLeftButtonUp">
                    
                    <!-- 节点内容模板 (Plan Explorer 专业操作符盒子) -->
                    <nodify:NodifyEditor.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:PlanNodeViewModel}">
                            <Grid>
                                <!-- Material Design 风格的节点卡片 -->
                                <Border MinWidth="140" MinHeight="75" 
                                        Background="{DynamicResource MaterialDesignPaper}" 
                                        BorderBrush="{Binding DynamicBorderBrush}" 
                                        BorderThickness="{Binding DynamicBorderThickness}" 
                                        CornerRadius="6"
                                        SnapsToDevicePixels="True">
                                    <Border.ContextMenu>
                                    <ContextMenu Style="{DynamicResource MaterialDesignContextMenu}">
                                        <MenuItem Header="复制节点全部信息" Click="CopyNodeInfo_Click" FontSize="13" Padding="12,6">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16" Foreground="{DynamicResource AccentBrush}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="2" Opacity="0.12"/>
                                    </Border.Effect>
                                    
                                    <!-- 节点 ToolTip 保留原有详细信息 -->
                                    <Border.ToolTip>
                                        <ToolTip Background="Transparent" BorderThickness="0" Padding="0" Placement="Right" ToolTipService.ShowDuration="60000" ToolTipService.InitialShowDelay="100">
                                            <Border Background="{DynamicResource SurfaceBrush}" 
                                                    BorderBrush="{DynamicResource AccentBrush}" BorderThickness="3,0,0,0"
                                                    CornerRadius="8" ClipToBounds="True" Margin="8">
                                                <Border.ContextMenu>
                                    <ContextMenu Style="{DynamicResource MaterialDesignContextMenu}">
                                        <MenuItem Header="复制节点全部信息" Click="CopyNodeInfo_Click" FontSize="13" Padding="12,6">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16" Foreground="{DynamicResource AccentBrush}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Border.Effect>
                                                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="4" Opacity="0.15"/>
                                                </Border.Effect>
                                                <StackPanel Width="310">
                                                    <!-- 头部区域 (无缝深色背景) -->
                                                    <Border Background="{DynamicResource AccentBrush}" Padding="10,8">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <StackPanel Grid.Column="0">
                                                                <TextBlock Text="{Binding PhysicalOp}" FontWeight="Bold" FontSize="15" Foreground="White" />
                                                                <TextBlock Text="{Binding LogicalOpSuffix}" FontSize="12" FontStyle="Italic" Foreground="#E0F0FF" Margin="0,2,0,0" Visibility="{Binding HasExtraInfo}"/>
                                                            </StackPanel>
                                                            <Border Grid.Column="1" Background="#33000000" CornerRadius="4" Padding="6,2" VerticalAlignment="Top">
                                                                <TextBlock Text="{Binding NodeId, StringFormat='Node {0}'}" FontSize="11" Foreground="White"/>
                                                            </Border>
                                                        </Grid>
                                                    </Border>
                                                    <!-- 主体数据区域 -->
                                                    <Border Padding="10">
                                                        <StackPanel>
                                                            <!-- 执行与行数信息 (两列网格排版) -->
                                                            <Grid Margin="0,0,0,12">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="140"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>
                                                                
                                                                <TextBlock Text="Executions (Act/Est):" Grid.Row="0" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1" Margin="0,3">
                                                                    <TextBlock Text="{Binding ActualExecutions}" FontSize="12" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"/>
                                                                    <TextBlock Text=" / " FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                                    <TextBlock Text="{Binding EstimatedExecutions}" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                                                </StackPanel>

                                                                <TextBlock Text="Rows (Act/Est):" Grid.Row="1" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1" Margin="0,3">
                                                                    <TextBlock Text="{Binding ActualRows}" FontSize="12" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"/>
                                                                    <TextBlock Text=" / " FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                                    <TextBlock Text="{Binding EstRows}" FontSize="12" Foreground="{DynamicResource TextBrush}"/>
                                                                </StackPanel>

                                                                <TextBlock Text="Data Size (Est):" Grid.Row="2" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding EstimatedDataSize}" Grid.Row="2" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextBrush}" />

                                                                <TextBlock Text="Execution Mode:" Grid.Row="3" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding ExecutionMode}" Grid.Row="3" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextBrush}" />
                                                            </Grid>

                                                            <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,12"/>

                                                            <!-- 成本信息 -->
                                                            <Grid Margin="0,0,0,12">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="140"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                </Grid.ColumnDefinitions>
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                    <RowDefinition Height="Auto"/>
                                                                </Grid.RowDefinitions>
                                                                
                                                                <TextBlock Text="Operator Cost:" Grid.Row="0" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding EstimatedOperatorCost}" Grid.Row="0" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextBrush}" />

                                                                <TextBlock Text="I/O Cost:" Grid.Row="1" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding EstimatedIOCost}" Grid.Row="1" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextBrush}" />

                                                                <TextBlock Text="CPU Cost:" Grid.Row="2" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding EstimatedCPUCost}" Grid.Row="2" Grid.Column="1" FontSize="12" Foreground="{DynamicResource TextBrush}" />

                                                                <TextBlock Text="Subtree Cost:" Grid.Row="3" Grid.Column="0" FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,3"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding EstimatedSubtreeCostStr}" Grid.Row="3" Grid.Column="1" FontSize="12" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" />
                                                            </Grid>

                                                            <!-- 数据库对象与详细属性 -->
                                                            <StackPanel Visibility="{Binding HasObjectDetails}" Margin="0,4,0,0">
                                                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,8"/>
                                                                <TextBlock Text="Object Details" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding ObjectDetails}" FontSize="12" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" />
                                                            </StackPanel>

                                                            <StackPanel Visibility="{Binding HasPartitionInfo}">
                                                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,8"/>
                                                                <TextBlock Text="Partitioning" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                                    <TextBlock Text="Partition Count: " FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                                    <TextBlock Text="{Binding PartitionCount}" FontSize="12" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                                    <TextBlock Text="Partition Range: " FontSize="12" Foreground="{Binding PartitionLabelColor}"/>
                                                                    <TextBlock Text="{Binding PartitionRange}" FontSize="12" Foreground="{Binding PartitionRangeColor}" FontWeight="SemiBold"/>
                                                                </StackPanel>
                                                            </StackPanel>

                                                            <StackPanel Visibility="{Binding HasOutputList}">
                                                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,8"/>
                                                                <TextBlock Text="Output List" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding OutputList}" FontSize="12" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" MaxHeight="80"  />
                                                            </StackPanel>

                                                            <StackPanel Visibility="{Binding HasSeekPredicates}">
                                                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,8"/>
                                                                <TextBlock Text="Seek Predicates" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding SeekPredicates}" FontSize="12" Foreground="{DynamicResource AccentHoverBrush}" TextWrapping="Wrap" MaxHeight="80"  />
                                                            </StackPanel>

                                                            <StackPanel Visibility="{Binding HasPredicate}">
                                                                <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,8"/>
                                                                <TextBlock Text="Residual Predicate" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,4"/>
                                                                <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding Predicate}" FontSize="12" Foreground="{DynamicResource AccentHoverBrush}" TextWrapping="Wrap" MaxHeight="80"  />
                                                            </StackPanel>

                                                            <!-- 警告信息 -->
                                                            <StackPanel Visibility="{Binding HasWarningVisible}" Margin="0,8,0,0">
                                                                <Border Background="#FFF3E0" BorderBrush="#FFB74D" BorderThickness="1" CornerRadius="6" Padding="12">
                                                                    <StackPanel>
                                                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                                            <materialDesign:PackIcon Kind="Alert" Foreground="#F57C00" Width="16" Height="16" VerticalAlignment="Center"/>
                                                                            <TextBlock Text="性能警告 (Warnings)" FontSize="12" FontWeight="Bold" Foreground="#F57C00" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                                                        </StackPanel>
                                                                        <TextBox IsReadOnly="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" Text="{Binding Warnings}" FontSize="12" Foreground="#D84315" TextWrapping="Wrap"/>
                                                                    </StackPanel>
                                                                </Border>
                                                            </StackPanel>
                                                        </StackPanel>
                                                    </Border>
                                                </StackPanel>
                                            </Border>
                                        </ToolTip>
                                    </Border.ToolTip>

                                    <!-- 重新设计的内部布局: 顶部色带 (DynamicBackgroundBrush) + 底部主体 -->
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="4" /> <!-- 顶部状态色条 -->
                                            <RowDefinition Height="Auto" /> <!-- 头部信息 -->
                                            <RowDefinition Height="*" /> <!-- 底部数据 -->
                                        </Grid.RowDefinitions>
                                        
                                        <!-- 状态指示条 (Cost 色或 Warning 色) -->
                                        <Border Grid.Row="0" Background="{Binding DynamicBackgroundBrush}" CornerRadius="6,6,0,0" />
                                        
                                        <!-- 警告与并行标记悬浮在右上角 -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,4,0" Panel.ZIndex="5">
                                            <Border Width="16" Height="16" CornerRadius="8" Background="{StaticResource CriticalBrush}"
                                                    Visibility="{Binding HasWarningVisible}" ToolTip="{Binding Warnings}">
                                                <TextBlock Text="!" Foreground="White" FontSize="11" FontWeight="Bold" 
                                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                                            </Border>
                                            <materialDesign:PackIcon Kind="LightningBolt" Width="14" Height="14" Foreground="{StaticResource WarningBrush}" Visibility="{Binding IsParallelVisible}" Margin="2,1,0,0"/>
                                        </StackPanel>

                                        <!-- Header 区: 图标与名称 -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,8,24,6">
                                            <!-- 图标 -->
                                            <Viewbox Width="20" Height="20" VerticalAlignment="Center">
                                                <Path Data="{Binding IconGeometry}" Fill="{Binding IconBrush}" Stretch="Uniform" />
                                            </Viewbox>
                                            
                                            <!-- 名称 -->
                                            <StackPanel Margin="6,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding PhysicalOp}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" MaxWidth="100" ToolTip="{Binding PhysicalOp}"/>
                                                <TextBlock Text="{Binding LogicalOpSuffix}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Visibility="{Binding HasExtraInfo}"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <!-- 分隔线 -->
                                        <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                                            <Grid Background="{DynamicResource MaterialDesignPaper}" Margin="0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <!-- 数据指标: 成本占比 -->
                                                <TextBlock Grid.Row="0" Text="{Binding PrimaryDisplayValue}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" Margin="0,6,0,2"/>
                                                <!-- 对象名称 -->
                                                <TextBlock Grid.Row="1" Text="{Binding ObjectDetails}" 
                                                           FontSize="9.5" Foreground="{DynamicResource SecondaryTextBrush}" 
                                                           HorizontalAlignment="Center" TextAlignment="Center"
                                                           MaxWidth="130" TextWrapping="Wrap" TextTrimming="WordEllipsis" MaxHeight="30" Margin="4,0,4,6"
                                                           Visibility="{Binding HasObjectDetails}"/>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </Border>
                                
                                <!-- 折叠/展开按钮 (悬浮在右下角) -->
                                <Button Content="{Binding CollapseButtonText}" 
                                        Visibility="{Binding CollapseButtonVisibility}"
                                        PreviewMouseLeftButtonDown="ToggleCollapse_PreviewMouseDown"
                                        Panel.ZIndex="9999"
                                        Width="20" Height="20" FontSize="16" Padding="0,-2,0,0"
                                        FontWeight="Bold"
                                        HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                        Margin="0,0,-10,-10" 
                                        Background="{DynamicResource MaterialDesignPaper}" Foreground="{StaticResource InfoBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                        Cursor="Hand"
                                        ToolTip="折叠/展开子树">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="10"/>
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#000000" BlurRadius="4" ShadowDepth="1" Opacity="0.15"/>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </nodify:NodifyEditor.ItemTemplate>

                    <!-- 节点容器样式 (位置 + 选择效果) -->
                    <nodify:NodifyEditor.ItemContainerStyle>
                        <Style TargetType="{x:Type nodify:ItemContainer}">
                            <Setter Property="Location" Value="{Binding Location, Mode=TwoWay}"/>
                            <Setter Property="MinWidth" Value="230"/>
                            <Setter Property="MinHeight" Value="70"/>
                            <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVis}}"/>
                            <EventSetter Event="MouseDoubleClick" Handler="Node_MouseDoubleClick"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#1976D2" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </nodify:NodifyEditor.ItemContainerStyle>

                    <!-- 连接线 (数据流向父节点，由子指向父，支持数据流厚度与估算偏差颜色，以及悬浮数据量数值标签与流向动画) -->
                    <nodify:NodifyEditor.ConnectionTemplate>
                        <DataTemplate DataType="{x:Type local:ConnectionViewModel}">
                            <Grid Opacity="{Binding Opacity}" Visibility="{Binding IsVisible, Converter={StaticResource BoolToVis}}">
                                <!-- 1. 背景管道连接线 (展示行数对应的粗细，以及估算偏差颜色) -->
                                <nodify:LineConnection Source="{Binding SourceLocation}" 
                                                   Target="{Binding TargetLocation}"
                                                   Stroke="{Binding StrokeBrush}"
                                                   StrokeThickness="{Binding ThicknessValue}"
                                                   
                                                   
                                                   ToolTip="{Binding ToolTipText}"/>

                                <!-- 2. 前景动画虚线 (实现类似商业 SQL Sentry Plan Explorer 的动态数据流流向效果) -->
                                <nodify:LineConnection Source="{Binding SourceLocation}" 
                                                   Target="{Binding TargetLocation}"
                                                   Stroke="{DynamicResource AccentHoverBrush}"
                                                   StrokeThickness="1.5"
                                                   StrokeDashArray="4,4"
                                                   ArrowEnds="None"
                                                   IsHitTestVisible="False"
                                                   Opacity="0.9">
                                    <nodify:LineConnection.Style>
                                        <Style TargetType="{x:Type nodify:LineConnection}">
                                            <Style.Triggers>
                                                <EventTrigger RoutedEvent="Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="StrokeDashOffset"
                                                                             From="100" To="0" Duration="0:0:3"
                                                                             RepeatBehavior="Forever"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </nodify:LineConnection.Style>
                                </nodify:LineConnection>

                                <!-- 3. 数据流行数浮动标签 (精确居中于连接线中点) -->
                                <!-- 采用圆角胶囊样式，更贴合现代 UI -->
                                <Border Background="{DynamicResource SurfaceBrush}" 
                                        BorderBrush="{Binding StrokeBrush}" 
                                        BorderThickness="1"
                                        CornerRadius="10" Padding="8,3"
                                        HorizontalAlignment="Left" VerticalAlignment="Top"
                                        IsHitTestVisible="False"
                                        Panel.ZIndex="10">
                                    <Border.RenderTransform>
                                        <TranslateTransform X="{Binding MidpointX}" Y="{Binding MidpointY}"/>
                                    </Border.RenderTransform>
                                    <Border.ContextMenu>
                                    <ContextMenu Style="{DynamicResource MaterialDesignContextMenu}">
                                        <MenuItem Header="复制节点全部信息" Click="CopyNodeInfo_Click" FontSize="13" Padding="12,6">
                                            <MenuItem.Icon>
                                                <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16" Foreground="{DynamicResource AccentBrush}"/>
                                            </MenuItem.Icon>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="6" ShadowDepth="2" Opacity="0.25"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="ArrowRight" Foreground="{Binding StrokeBrush}" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,4,0" RenderTransformOrigin="0.5,0.5">
                                            <materialDesign:PackIcon.RenderTransform>
                                                <RotateTransform Angle="{Binding ArrowAngle}"/>
                                            </materialDesign:PackIcon.RenderTransform>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding LabelText}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 5. 绝对置顶的真实独立箭头层 -->
                                <Polygon Points="{Binding ArrowPoints}" Fill="{Binding StrokeBrush}" 
                                         HorizontalAlignment="Left" VerticalAlignment="Top"
                                         IsHitTestVisible="False" Panel.ZIndex="20">
                                    <Polygon.RenderTransform>
                                        <TranslateTransform X="{Binding ArrowTransformX}" Y="{Binding ArrowTransformY}"/>
                                    </Polygon.RenderTransform>
                                </Polygon>
                            </Grid>
                        </DataTemplate>
                    </nodify:NodifyEditor.ConnectionTemplate>
                </nodify:NodifyEditor>

                <!-- 空状态提示 -->
                <TextBlock x:Name="EmptyHint" Text="尚未加载执行计划&#x0A;请打开 .sqlplan 或 .xml 执行计划文件&#x0A;（打开后会自动切换到此专业 Nodify 盒子视图）" 
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           TextAlignment="Center" Foreground="#546E7A" FontSize="13"
                           Visibility="Collapsed"/>
            </Grid>
        </Border>
    
        <!-- 漂亮的 Toast 提示窗 -->
        <Popup x:Name="ToastPopup" Placement="Bottom" VerticalOffset="-80" HorizontalOffset="0" AllowsTransparency="True" StaysOpen="False" IsHitTestVisible="False" PopupAnimation="Slide">
            <Border Background="#2C2C2C" CornerRadius="6" Padding="16,10" Margin="10">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="12" ShadowDepth="4" Opacity="0.4"/>
                </Border.Effect>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="#4CAF50" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="节点信息已复制到剪贴板" Foreground="#FAFAFA" FontSize="13" FontWeight="Normal" VerticalAlignment="Center" Margin="0,0,4,0"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
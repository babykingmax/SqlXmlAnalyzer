# 检索来源与验证记录

## IMP-18 / IMP-19 联合审查修复（2026-09-09）

核对 Microsoft sql_variant、Stream.Read 文档、查询超时支持知识库及 dotnet/runtime v8.0.0 编码器源码。采用 UTF-16 码元保真、sql_variant 支持范围明确拒绝、有界读取前置、建库结果不确定时独立清理及真实提交状态展示。新增 47 项回归，Debug/Release 各 2229 项通过；来源链接、两版本数据库反例及运行记录见 [联合审查修复](IMP-18-19联合审查修复与加固说明.md)。

## IMP-19 审查修复（2026-09-09）

本轮核对 Microsoft 的 @@ROWCOUNT、@@ERROR、SqlDataReader.RecordsAffected 文档及 dotnet/SqlClient v6.1.6、Microsoft SqlScriptDOM 官方源码，修复内部 SELECT 污染瞬时状态、观察器写操作擦除差异及 -1 哨兵值抵消 DML 计数。GitHub 连接器未提供可调用工具，改用直接 HTTPS 读取官方公开来源。出处、版本、修复前失败测试及修复后实际数据库/应用证据见 [IMP-19 审查修复与加固](IMP-19审查修复与加固说明.md)。

## IMP-19 后续验证（2026-09-09）

[SQL 语义验证与可靠应用说明](IMP-19SQL语义验证与可靠应用.md)记录了本轮官方来源、实现边界、脚本与实际输出。核对 Microsoft 的 LTRIM、table 类型、SAVE TRANSACTION、EXECUTE AS、XACT_STATE 文档及 MicrosoftDocs/SqlScriptDOM 官方源码；采用独立新库和不可恢复的本库用户上下文，不以最终 ROLLBACK 代替隔离。

本轮实际环境为 SQL Server 2019 LocalDB `15.0.4382.1` / 兼容级别 150 和 SQL Server 2025 LocalDB `17.0.4075.5` / 170，覆盖 CI/CS 排序规则及固定会话设置。21 个 SQL 场景在两版本上运行，Debug/Release 各 42/42 预期断言通过；反例中成功观察到 `Different` 不表示改写等价。CLI 实际审核应用及 WPF 生产窗口验证、确认、带备份写入和四张截图通过。全量构建各 0 警告/错误、单元测试各 2141 通过，包含未知错误 DUMP 和分模式日志。后文“未执行数据库/UI”等限制是原基线记录，当前本步范围及未证明性质以上述说明为准。

## 1. 基线、范围与方法

- 日期：2026-09-08，Asia/Taipei。
- 仓库：`E:\SqlXmlAnalyzer`；HEAD：`c5b1e0766cfcdf28f0d3a27d3238f5f0d6ae1a15`。
- 首先执行 `git ls-files`，再按列出的明确路径读取/检索；未递归列出根目录或扫描生成目录。
- 检索范围：415 个跟踪的 `.cs`、`.xaml`、`.csproj` 文件，约 46,249 非空文本行；包括约束、构建配置和规则配置的额外读取。
- 全范围静态检索用于定位 XML 入口、异步/取消、规则、文件写入、外部进程等；核心数据流、算法、规则、改写、导出及视图进行重点精读，并核对相关测试和调用处。没有宣称逐行阅读所有 46,249 行或审查第三方二进制。

已读取用户指定 [review-agent 技能](C:/Users/Administrator/.codex/skills/.system/review-agent/SKILL.md)，采用缺陷优先、证据和调用路径验证、按 P1/P2 排列的方式。该技能默认面向变更集、禁止改文件和委派；本次用户明确要求审查整个项目并输出文档，因此仅为交付新增 DOCS 文件，应用源码保持只读，也未委派子代理。当前状态缺陷不归责于某次新提交。

工作树原有变更是 `AGENTS.md`、`MainWindow.Services.cs`、`Views/ShellNavigationRail.xaml` 和未跟踪 `MainWindow.KeyboardShortcuts.cs`；已阅读相关 diff/源码，交付时保留。这些变更包含导航可访问名称和快捷键，未列为缺失能力。

旧的 `CODE_REVIEW_*`、阶段整改说明没有被当成本次缺陷结论来源；本次问题由当前代码、独立探针和外部文档重新建立证据。

## 2. 检索策略与工具限制

按用户要求优先使用 Microsoft 官方文档/知识库，其次 SQL Server Support/CSS、公开支持案例，再补充作者社区文章及 GitHub 成熟实现。

本会话网页检索工具多次连接失败。随后直接使用 Microsoft Learn 的可读取正文、官方网页、GitHub 公共 REST/raw 接口获取并核验内容。也读取了 GitHub 插件的 github 技能；本会话没有暴露可调用的 GitHub connector 方法，因此使用其允许的公开接口替代。没有把检索摘要或未经打开的 URL 当作已核实资料。

所有下列页面均于 2026-09-08 成功读取。只把内容用于支持相应结论，不代表 Microsoft 或开源作者认可本工具。博客/KB 的历史版本边界按原文保留。

## 3. 资料索引与适用范围

### 3.1 Microsoft 官方文档与知识库

| 来源 | 本次支持的结论 | 对应文档/问题 |
| --- | --- | --- |
| [Deadlocks guide](https://learn.microsoft.com/en-us/sql/relational-databases/sql-server-deadlocks-guide) | process/resource/victim 结构、priority、死锁处理和调查边界 | R05/R06/R22，死锁模型 |
| [Execution plans](https://learn.microsoft.com/en-us/sql/relational-databases/performance/execution-plans) | 估算与实际计划的信息差异 | R15，指标表达 |
| [Tune nonclustered indexes with missing index suggestions](https://learn.microsoft.com/en-us/sql/relational-databases/indexes/tune-nonclustered-missing-index-suggestions) | 建议来自优化阶段，有列序与估算等限制，需要后续设计验证 | 索引沙盒、对象模型、UI-09 |
| [SET TRANSACTION ISOLATION LEVEL](https://learn.microsoft.com/en-us/sql/t-sql/statements/set-transaction-isolation-level-transact-sql) | REPEATABLE READ 与 SERIALIZABLE 的区别 | R07 |
| [LTRIM](https://learn.microsoft.com/en-us/sql/t-sql/functions/ltrim-transact-sql) | 删除前导空格的实际语义 | R02 |
| [table (Transact-SQL)](https://learn.microsoft.com/en-us/sql/t-sql/data-types/table-transact-sql) | 表变量作用域、回滚及兼容级别相关优化 | R19，改写安全域 |
| [Database identifiers](https://learn.microsoft.com/en-us/sql/relational-databases/databases/database-identifiers) | 分隔标识符、转义与对象名称规则 | R13 |
| [Showplan logical and physical operators reference](https://learn.microsoft.com/en-us/sql/relational-databases/showplan-logical-and-physical-operators-reference) | 算子属性、行数和执行形态的调查背景 | 指标模型；不单凭文档推测本程序字段读取结果 |
| [WPF threading model](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/threading-model) | Dispatcher/UI 线程与后台计算边界 | 架构性能设计 |
| [UI Automation of a WPF custom control](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/controls/ui-automation-of-a-wpf-custom-control) | 自定义控件通过 AutomationPeer 提供可访问语义 | UI 键盘/辅助技术 |

以上为 SQL/.NET 的规范与产品文档。具体代码缺陷仍由代码和复现输出证明，例如 R09 的并行数值错误不是仅凭一篇介绍文档推断。

### 3.2 SQL Server Support/CSS 与公开支持案例

| 来源 | 核对内容 | 使用限制 |
| --- | --- | --- |
| [How to Capture Running Queries in SQL Server](https://techcommunity.microsoft.com/blog/sqlserversupport/how-to-capture-running-queries-in-sql-server/4454947) | SQL Server Support Blog，2025-09-30；提供支持案例使用的 XE 采集脚本与事件关联信息 | 支持扩展诊断数据包的设计；不在本次自动部署其采集脚本，不对所有负载承诺零开销 |
| [KB4089473: Better intra-query parallelism deadlocks troubleshooting](https://support.microsoft.com/en-us/topic/kb4089473-better-intra-query-parallelism-deadlocks-troubleshooting-in-sql-server-2017-and-2016-c0cc8ac0-ae4f-966a-b6ed-29dfcd17972a) | 公开知识库案例，SQL Server 2016/2017 的 Exchange/SyncPoint 诊断增强 | 用于确定需要保留的字段；不是要求对任意当前服务器安装历史补丁 |
| [Troubleshoot memory grant issues](https://learn.microsoft.com/en-us/troubleshoot/sql/database-engine/performance/troubleshoot-memory-grant-issues) | Microsoft 排障文档中的内存授予、等待、计划证据与排查过程 | 支持把事实、假设及进一步调查分开；不把所有 Spill 简化为服务器物理内存不足 |
| [Understanding SQL server memory grant](https://techcommunity.microsoft.com/t5/sql-server-blog/understanding-sql-server-memory-grant/ba-p/383595) | SQL Server Engine 团队文章，原发表于 2010；required/additional memory 和并行上下文 | 这是 SQL Server Blog，不能误称 CSS 原创；历史适用版本为 SQL 2005/2008，只用于概念背景 |

其中 Support Blog 的作者明确说明其脚本用于客户支持调查，满足公开支持经验的检索需求；未访问任何私有 CSS 案例库。

### 3.3 社区作者资料

[Jonathan Kehayias：The Accidental DBA — Day 29 — Troubleshooting Deadlocks](https://www.sqlskills.com/blogs/jonathan/the-accidental-dba-day-29-of-30-troubleshooting-deadlocks/) 用于补充从 process-list/resource-list 跟踪依赖的调查方式，属于作者的一手技术文章。其年代较早；受害者选择、现代 XE/版本行为以当前 Microsoft 文档为准。未使用搜索结果中的培训广告、二手转述或未经核验的 Stack Overflow 回答支撑结论。

### 3.4 GitHub 成熟实现

| 项目与固定版本 | 实际查看内容 | 可借鉴内容及边界 |
| --- | --- | --- |
| [SQL Server First Responder Kit / sp_BlitzLock](https://github.com/BrentOzarULTD/SQL-Server-First-Responder-Kit/blob/756206859c23aa98cdb41643763c5f1d3c10cbab/sp_BlitzLock.sql) | main 提交 `756206859c23aa98cdb41643763c5f1d3c10cbab` 的 SQL 文件；victim、事件时间、exchangeEvent 等提取 | 借鉴证据字段、调查分组；本项目的 “[sp_BlitzLock 诊断]”文字不证明实际调用了该实现。未复制第三方代码进产品 |
| [Microsoft SqlScriptDOM](https://github.com/microsoft/SqlScriptDOM/tree/b583737682dbb1682b97d0ac5a5261dc94279b8d) | 官方 README、用途/许可证及仓库元数据 | 继续使用 AST 和访问器；语法库不提供 SQL 语义等价证明 |
| [Nodify](https://github.com/miroiu/nodify/tree/ce1ca77e409eb46543f0e7b8fd1ee34d2c73f531) | 仓库元数据、默认分支和许可证；未完整审查内部实现 | 现有项目已引用 Nodify 6.0.0，可沿用图交互框架；不能据此声称其为本项目提供了经过验证的虚拟化性能 |

FRK 的 GitHub 默认分支当时为 dev；上述实际读取并固定引用的是 main，未混用 dev 提交。GitHub API 对其许可证返回 NOASSERTION，因此没有自行宣称 MIT；若未来移植代码，应读取该提交的实际许可证。SqlScriptDOM 和 Nodify 元数据/README 为 MIT。

### 3.5 UI 标准补充

[W3C：Understanding Contrast (Minimum)](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html) 用于普通文字 4.5:1、大号文字 3:1 的设计验收目标。此为独立的视觉标准来源，不属于 SQL Server 诊断证据，也不表示当前 WPF 已通过可访问性认证。

## 4. 已运行的验证

### 4.1 构建与现有测试

环境 SDK：`dotnet --version` = `10.0.400`；项目仍以自身 `net8.0` / `net8.0-windows` 目标编译。

```powershell
dotnet build SqlXmlAnalyzer.sln --nologo
dotnet test SqlXmlAnalyzer.Tests\SqlXmlAnalyzer.Tests.csproj --no-build --nologo `
  --logger 'trx;LogFileName=review-20260908.trx' `
  --results-directory "$env:TEMP\SqlXmlAnalyzerReview-20260908\test-results"
```

| 验证 | 实际结果 |
| --- | --- |
| 全解决方案构建（含还原） | 成功；0 警告、0 错误；命令报告约 20.82 秒 |
| xUnit/FluentAssertions | 807 通过、0 失败、0 跳过；测试摘要约 2 秒 |
| 原始 TRX | `C:\Users\Administrator\AppData\Local\Temp\SqlXmlAnalyzerReview-20260908\test-results\review-20260908.trx` |
| 覆盖率 | 未生成；不作覆盖率结论 |
| WPF UI 实机 | 未运行/未截图；UI 仅静态布局、绑定与交互路径审查 |
| SQL Server 数据库验证 | 未连接实例；未执行生成 SQL 或 DDL |

本次只交付文档，未为文档内容修改原有测试。复现探针用于验证新发现的领域反例；其结果不包含在 807 项测试总数中。

### 4.2 独立探针

运行真实项目类型的临时 .NET 8 Windows 控制台程序，检查 20 个场景。附件源文件使用 `.cs.txt` 扩展名，避免被根 WPF 项目的默认 Compile glob 意外编译。

```powershell
powershell -File .\DOCS\review-evidence\Run-ReviewProbe.ps1
# 也可在 PowerShell 7 中：
pwsh -File .\DOCS\review-evidence\Run-ReviewProbe.ps1
```

脚本在系统临时目录下创建唯一工作目录，引用当前仓库项目；没有连接数据库、启动 WPF 主窗口、写回 SQL 或修改应用源码。它生成 `probe-results.json`，并打印其路径。首次运行可能需要 NuGet 还原；为匹配依赖，临时项目显式引用 System.Text.Json 10.0.9。准备探针时曾修正过临时 runner 的该依赖，未将 runner 配置问题计为产品缺陷。

| 探针 | 关键实际观察 |
| --- | --- |
| R01 | 脱敏后 ConstValue/Server/Alias 标记保留 |
| R02 | LTRIM 被删除、IsSuccess=true，有前导空格假设警告 |
| R03 | 模拟备份失败，仍 SourceWritten=true、IsSuccess=true |
| R04 | 非计划 XML 返回零问题；另用真实 CLI 验证 Passed/退出 0 |
| R05 | 包装死锁分类 Unknown，但解析器接受 |
| R06 | priority=7 被解析为 0 |
| R07 | REPEATABLE READ + S/X 命中 SERIALIZABLE Range Lock 模式 |
| R08 | 重叠环节点 c 在主图中，在时间线中却不属于环 |
| R09 | 总行数正确的并行计划被 RULE_030 报为 16 倍偏差 |
| R10 | 标准 IndexScan/Predicate 被通用残差提取漏掉 |
| R11 | Batch/Parallel=true/Ordered=true 变成 Row/false/False |
| R12 | 相同语句仅交换顺序，模拟收益 100% 与 65% |
| R13 | `[Order.Detail]` 被错误拆成 `[Order].[Detail]` |
| R14 | 不同 schema 的同名表均匹配首个 schema 建议 |
| R15 | 估算计划缺失指标被输出为相对实测 -100ms/-200 reads |
| R16 | 第二语句成本 2→999，没有出现在只取首根的比较中 |
| R17 | 父 Nested Loops 被赋予子 Seek 的对象和谓词 |
| R18 | 单一双进程环被计为两个环 |
| R19 | 含 ROLLBACK 的表变量代码仍无警告转换为临时表 |
| R20 | 已有 #T 与 @T 被转换成两个 CREATE TABLE #T |

探针是行为记录程序，不是声称已经修复问题的测试套件。20 条记录也不等于 20 个数据库集成测试。R19/R20 的 SQL 编译绑定/执行结果仍应在受控 SQL Server 环境中追加验证；R21 的 XAML 绑定和 R22 的合成排序由静态代码直接核对。

### 4.3 证据文件

- [ReviewProbe.cs.txt](./review-evidence/ReviewProbe.cs.txt)：已运行探针源代码。
- [Run-ReviewProbe.ps1](./review-evidence/Run-ReviewProbe.ps1)：可移植到当前仓库的运行脚本。
- [probe-results.json](./review-evidence/probe-results.json)：本次真实输出，22 项 Review 中前 20 项的证据。
- [baseline.json](./review-evidence/baseline.json)：HEAD、原有工作区变更、SDK、测试统计及关键文件 SHA256。

### 4.4 交付检查

交付版 `Run-ReviewProbe.ps1` 已从 DOCS 实际运行成功，生成的 20 条 JSON 记录与附件逐字节 SHA256 一致。五份 Markdown 的本地链接及代码行号范围检查通过，代码 Review 统计为 5 项 P1、17 项 P2；代码块围栏配对完整。关键源码指纹在写文档前后未变化，Git 工作区仅新增 DOCS，原有变更保留。

## 5. 尚未证明的事项

没有开展真实数据库版本矩阵、Query Store/在线 DMV 集成、恶意超大文件压测、WPF 可访问性自动化、DPI 截图比对、整包发布或第三方许可证法律审查。文档中的这些内容为明确的后续设计/验收任务，不算已经完成的测试，也不用于虚增缺陷数量。

若后续代码发生变化，应先用 baseline.json 的指纹和 HEAD 核对审查基线，再运行探针；行号仅对本次工作树有效。修复后应更新探针对应的回归测试与文档状态，而不是保留“已确认仍存在”的旧结论。

## IMP-05 来源补充（2026-09-08）

字段目录来自 Microsoft [SQL Server 2022 ShowPlan XSD 1.571](https://schemas.microsoft.com/sqlserver/2004/07/showplan/sql2022/showplanxml.xsd)，使用 PowerShell 下载并保存 SHA-256。未带版本的旧 URL 首次请求返回 404，未作为成功来源；正确来源及副本见 [IMP-05 说明](./IMP-05脱敏覆盖与输出入口验证.md)。该 XSD 用于字段名及基础类型分类，不代表全部输入已通过 XSD 或 SSMS 验证；新实现不在运行时下载 schema。

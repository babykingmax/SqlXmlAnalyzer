# 执行计划工作区任务型验收矩阵

更新日期：2026-09-11。目标和实施顺序以 [设计与差距评估](../../plan-workspace-design-and-gap-assessment.md) 为准；[工作区历史记录](README.md)及[节点、缩放、布局记录](graph-polish.md)保留原始证据。本次整理文档，**未实施 PW-01～PW-06，未新增人工可用性或实机验收**。

## 状态与证据口径

| 状态 | 含义 |
| --- | --- |
| 自动已通过 | 指定版本、输入和断言通过；仅覆盖该检查描述，不自动关闭相关 PW 任务 |
| 已知不足 | 已有代码或画面证据显示目标未满足；仍为开放问题，不能因为其他检查通过改称有保留地完成 |
| 待实机 | 需要真实显示设备、输入设备、辅助技术或专家任务观察；离屏截图不能代替 |
| 未实现 | 主设计文档中的新方案尚未落地；已有基础组件不等于该方案已实现 |

2026-09-10 的历史自动化结果为 Debug / Release 全量测试各 **3066 通过、0 失败、0 跳过**；对应构建均 **0 警告、0 错误**。Release 离屏记录包含 **24 组矩阵、1284 项断言、36 张截图、0 绑定错误**。这些数字不是任务成功率、文字可读性、诊断准确率或本轮发布提交的复跑成绩。原始入口为 [测试摘要](graph-polish-test-results.json)和[离屏验证记录](runs/20260910T234426-Release/verification.json)。

本轮发布前构建、测试及提交文件证据以 [publish-validation.json](publish-validation.json) 的实际内容为准；未附记录时不推断新结果。复跑通过也不能自动关闭下列已知不足或未实现项。

## 工作区任务矩阵

以下是待实施方案的验收脚本；“既有自动检查”列只说明可复用基础，不表示整行通过。

| ID / 当前状态 | 需要用户完成的任务与输入 | 既有自动检查及其边界 | 后续通过条件 / 必需证据 |
| --- | --- | --- | --- |
| **PW-01 画布与可读性**：已知不足；新方案未实现；待实机 | 在普通和窄窗口打开计划，辨认指定算子及对象、读取主指标、定位问题，再返回结构概览。覆盖 100 / 1000 / 5000 节点及 64 节点当前页 | 自动已通过：已测节点包围框、连续 fit 幂等、缩放中心、分页导航。**640 × 360 DIP 的 64 节点页约 0.1666% 是可读性失败**；图控件高度含工具栏与分页，不是净画布 | 分别记录净画布、工具区、分页区、比例和节点显示尺寸；区分完整概览与可读查看。用户能读对象和指标，能找到目标并返回上下文。窄窗提供可达的阅读路径；不能以“全图都在窗口里”代替任务通过 |
| **PW-02 诊断范围徽标**：已知不足；新方案未实现；待专家任务 | 在含算子、语句、计划和文档级问题的计划中，解释每个徽标表示谁的问题，并从算子问题一次定位。加入跨语句重复 NodeId | 自动已通过：完整 PlanLocation 定位及重复 NodeId 基础回归。已知分配逻辑会把无 Operator 的诊断放到首算子；历史 Stream Aggregate 的 3 个警告包含语句级索引建议和文档 CPU 提示，不能当作节点归属正确的证据 | 无明确算子归属的问题不增加首节点徽标数；图、热点、详情及问题导航的数量口径一致，诊断总数与检查记录不丢失。用户能区分所属范围，不把全局线索误判为该节点故障 |
| **PW-03 问题摘要**：已知不足；新方案未实现；待专家任务 | 从同名基数偏差、隐式转换及索引建议中选出指定对象的问题；说明一条关键事实，再打开完整证据或 DDL | 自动已通过：真实问题与 Skipped / Failed 分流、搜索及严重度筛选。历史摘要缺对象或截断在 CREATE / GUID 名称处；“有 Summary 绑定”不等于摘要可理解 | 卡片稳定显示对象、问题和结构化关键事实，并保留严重度、置信度与范围。窄侧栏可区分同名项；DDL 不淹没首屏，完整脚本和证据仍可访问 |
| **PW-04 详情与统计证据表达**：已知不足；新方案未实现；待专家任务 | 比较零问题节点、有诊断节点、估算计划、实际计划、部分运行数据和真实零值；判断统计画面是未导入、真实导入还是教学示意 | 自动已通过：部分事实口径、缺失值与零值、绑定和证据联动。现有重复空段、原始单位及教学图默认表达仍有误读风险；0 绑定错误不证明来源表达清楚 | 零问题节点以事实为主，空原因 / 建议 / 证据不重复占位；实际、估算、无效和未采集可区分。用户不需靠小字猜统计来源；真实统计保留导入来源和适用范围，不将教学图或估算评分解释为实测收益 |
| **PW-05 命令与工作流**：已知不足；新方案未实现；待实机 | 用鼠标和实体键盘分别完成“问题 → 节点 → 证据 → 索引审核或 A/B → 导出”，期间开关侧栏、调整排列、恢复默认布局并返回原选中项 | 自动已通过：已测按钮 / 路由按键、fit / 100% / 定位、最新视口操作优先及部分审核流程。重复入口、面板开关状态和“布局”命名仍不清楚；路由事件不等于完整键盘任务 | 用户能从标签预测动作，侧栏状态明确；“恢复默认布局”和“水平 / 垂直排列”不混淆。操作不意外丢失筛选、选择及偏好；审核、取消、返回和导出状态可追踪，无焦点陷阱 |
| **PW-06 在线采集与实验历史**：后续；未实现 | 在另行确定的受控 SQL Server 环境采集计划与执行指标，记录参数和环境，取消一次采集，再比较多个实验版本 | 本矩阵没有该工作流的通过证据。既有离线实际计数、快照比较、会话保存及隔离 LocalDB 语义验证，不等于通用在线采集或受控性能实验 | 先确定支持环境与采集边界，再验证来源、环境、参数、会话设置、计划身份、指标可用性、取消 / 错误、版本评论和比较条件。区别单次观察与重复实验，不把估算成本下降当作实测性能改善 |

实施顺序为 PW-01～PW-03、PW-04～PW-05，最后另行建设 PW-06。每个 PW 项必须分别具备实现证据、针对性自动回归和任务结果，才能改为完成；不能将整份矩阵一次标为通过。

## 跨场景回归与待验输入

| 场景 | 当前证据 / 状态 | 下一次必须核验的任务 |
| --- | --- | --- |
| 100 / 1000 / 5000 节点 | 自动已通过：合成计划加载、64 节点上限、查找和末页定位；布局服务包含深链与分支用例。性能任务与真实复杂计划阅读仍待验 | 三种规模分别记录首个可用画面、目标定位、输入响应、取消及切换后的选择稳定性；选择不在当前页的目标。不得将一次耗时观测当作 SLA 或性能改善结论 |
| 多批次、多语句重复 NodeId | 自动已通过：完整身份链路基础回归；PW-02 新范围数量语义未实现 | 选择另一个语句中同 NodeId 的问题，核对语句、节点、右侧证据和来源；返回后仍指向正确身份 |
| 折叠节点与分页边界 | 自动已通过：折叠、展开、跨页导航、隐藏计数、边界入口及最新操作优先的已测路径 | 点击折叠或跨页目标一次到达；解释当前页以外的输入 / 输出，不把分页边界理解为执行计划结束；折叠后再次读取相关问题 |
| 单个真实问题 + 2000 条 Skipped | 自动已通过：真实问题仍保留在问题列表、检查记录分开可达 | 专家在不阅读全部日志的情况下找到真实问题，同时说明分析是否完整；Failed 检查不应被误认为 SQL 的严重故障 |
| 估算 / 实际 / 部分运行信息 / 真实零值 | 自动已通过：部分字段口径、不可比偏差及零值回归；PW-04 呈现改造未实现 | 在图、热点和详情之间核对同一事实，解释总行与每次执行、成本分母、不可比较原因；不知道的数据不得被解读为零 |
| 浅色 / 深色及主题切换 | 自动已通过：历史离屏模板、选择样式、提示框切换和绑定检查；物理显示效果待实机 | 验证正常文字、问题徽标、选中状态、焦点及提示框可读；颜色不能成为辨别范围或严重度的唯一线索 |
| 1280×720 / 1366×768 / 1920×1080，100% / 125% / 150% / 200% | 自动已通过：历史 24 组等效 DIP / 位图矩阵，不是逐个物理 DPI 实机验证 | 真实 Windows DPI 下完成 PW-01～PW-05；记录 OS 缩放、屏幕分辨率、实际窗口尺寸和净画布。跨显示器切换及缩放变化后复核控件位置、焦点和阅读路径 |
| 640 × 360 DIP、64 节点页 | **已知不足：约 0.1666% 不可读**；几何通过不改变失败状态 | 保留该反例复测。用户必须有清楚且可达的概览 / 阅读切换路径，能找到、读懂并返回指定节点；不要求在同一小画布上同时读完所有节点 |
| 实体键盘与屏幕阅读器 | 待实机；已测 WPF 路由事件仅为自动化基础 | 仅用键盘完成查找、分页、问题定位、折叠、详情、返回与审核入口；可见焦点、Tab 顺序、Esc 返回和朗读名称应与真实状态相符 |

输入应同时包含合成计划和经适当脱敏、获准使用的真实计划。历史探针的 100 / 1000 / 5000 节点及小视口数据为合成输入；没有在本轮连接数据库或重新采集计划。

## DBA / SQL 开发者人工任务记录

下面任务均为 **待验**，不是已完成的用户测试。参与者应使用自己的操作顺序；观察者记录误判、回退和求助，不替参与者完成关键步骤。任务时间先如实记录，没有经确认的阈值时不得宣布速度达标。

| 任务 | 参与者必须给出的结果 | 记录重点 / 对应 PW |
| --- | --- | --- |
| 判断调查入口 | 当前语句、需要优先调查的问题、分析完整性 | 是否误读检查失败或全局提示；PW-02、PW-03 |
| 定位并解释节点 | 正确节点身份、对象、主指标、诊断证据及其范围 | 重复 NodeId、折叠、跨页是否造成误定位；PW-01、PW-02 |
| 解释数据限制 | 为什么该指标可比或不可比，统计画面来自哪里 | 未采集、实际零值、教学示意及估算收益的误读；PW-04 |
| 完成一次调查链 | 从问题到证据，再到审核 / 比较或导出，并说明操作结果 | 入口可发现性、返回路径、审核状态、取消后恢复；PW-05 |
| 仅用实体键盘重做关键步骤 | 同一目标、同一证据及正确返回位置 | 焦点顺序、快捷键、遮挡和陷阱；PW-01、PW-05 |

每份记录至少包含：验证时间、源码提交与构建标识、输入及校验值、合成 / 真实来源、主题、实际分辨率与 OS DPI、窗口 DIP、净画布尺寸、显示比例、节点数、操作步骤、预期与实际结果、任务耗时、误判 / 求助、截图或录屏、验证者及未通过原因。没有真实设备信息的结果只能归类为离屏或模拟验证。

## 历史 DUMP 停滞与结果保留

一次历史 Debug 全量运行在既有 `ReviewWorkspaceDiagnosticTests.UnknownIndexRefreshFailure_ClearsBothScriptsAndReportsDump` 自转储路径附近停止推进；2340 项通过、0 项失败的中止记录保留在仓库本机路径 `.tmp.plan-ui-test-results/plan-graph-polish-debug-aborted.trx`。当时新增 16 项视口交互测试已完成；异常 sidecar 与 0 字节 `process.dmp.partial` 是定位线索，并非已经完成原生死锁诊断。

随后带 `--blame-hang --blame-hang-timeout 120s` 的 Debug 完整复跑 3066 项通过；历史 Release 同样 3066 项通过。完整说明和本地日志入口见 [历史回归记录](graph-polish.md#历史构建与全量回归结果)。**复跑成功不证明 DUMP 问题已修复**，本轮没有修改其实现，也不能删除中止记录使验证历史看起来全部顺利。

后续若再次发生，应保留对应测试活动、日志、sidecar 及原生采集证据，再单独处理。原始 DUMP 可能包含进程数据，保留本机调查材料；本矩阵不要求将其作为公开文档附件。

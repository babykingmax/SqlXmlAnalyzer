<?xml version="1.0" encoding="utf-8"?>
<PendingCommit>
  <CommitComment>  为了实现专业的多标签页功能，我主要完成了以下底层架构和测试的建设：

  ### 1. 构建多标签页的 ViewModel 架构

  在  SqlXmlAnalyzer.Core.ViewModels  中，引入了标准的 MVVM 标签页管理模型：

  • 新增  DocumentTabViewModel （作为抽象基类，包含了  Title 、 FilePath  以及关闭标签的  CloseCommand  逻辑）。
  • 新增  DeadlockTabViewModel  和  PlanTabViewModel ，分别继承基类，用于承载死锁 XML 和执行计划 XML 的独立数据环境。
  • 在主视图模型  MainViewModel  中，添加了  ObservableCollection&lt;DocumentTabViewModel&gt; Tabs  以及  SelectedTab  属性，用于管理所有打开的文档。

  ### 2. 编写并跑通 xUnit 单元测试

  根据你的要求，我为标签页管理功能编写了相应的测试，并修复了测试工程与 WPF 工程之间的引用隔离问题（升级到了  net8.0-windows  并启用了 WPF 测试支持）：

  •  MainViewModel_InitializesWithEmptyTabs ：测试默认初始化状态。
  •  AddTab_SelectsNewTab ：测试成功添加新计划/死锁标签并自动聚焦。
  •  CloseCommand_RemovesTabFromCollection ：测试触发  CloseCommand  时标签能够正确地从集合中销毁并重置焦点。

  │ 当前  dotnet test  执行结果：已通过! (12个测试用例全绿通过)。

  ### 3. 开始拆解  MainWindow  (UI 解耦准备)

  由于此前所有关于  死锁分析  和  执行计划分析  的 UI 控件（接近 2000 行代码）全部硬编码在  MainWindow.xaml  和  MainWindow.xaml.cs  中，如果直接绑定动态的  Tabs  会导致控件名称找不到（DataTemplate 隔离）。
  因此，我已经为你创建了第一个独立的组件文件  Views/DeadlockView.xaml  和对应的代码隐藏文件，将原来的死锁布局彻底剥离了出来。
  ──────</CommitComment>
  <PinnedBranches />
  <PublishPrompt Enabled="True" />
  <ActiveAccountPrompt Enabled="True" />
  <RepositorySorts />
  <CreatePullRequest_DefaultTargetBranch />
</PendingCommit>
namespace SqlXmlAnalyzer.Core.Services
{
    /// <summary>
    /// Shared outer dimensions of an execution-plan card, in WPF device-independent units.
    /// Layout, viewport bounds and connection anchors must use the same dimensions.
    /// </summary>
    public static class PlanGraphNodeMetrics
    {
        public const double Width = 260;
        public const double Height = 88;
    }
}
